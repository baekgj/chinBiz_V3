====================================================================
 친비즈(ChinBiz) V3 · 제작 노트
 작성 기준일: 2026-07-19
 스택: Spring Boot 3.5.16(BE:9001) + Next.js 16(HOME:8001 / ADMIN:3100) + MySQL8(chin4)
====================================================================

────────────────────────────────────────────────────────────────
[0] 인프라 / 환경 (배포·로컬 전환)
────────────────────────────────────────────────────────────────
● 배포 후 로그인 실패("백엔드(9001) 실행 여부 확인") 원인 = FE가 브라우저에서
  http://localhost:9001 을 호출 → 접속 PC 자신의 localhost를 찾아 실패.
● 해결: FE API 주소를 "접속한 host 기준 런타임 자동 해석"으로 변경.
  - home/src/lib/api.ts, admin/src/lib/auth.ts 에 resolveServiceUrl(env, port, fallback) 추가.
    env(NEXT_PUBLIC_*) 지정 시 우선, 없으면 window.location.host + 지정포트(9001/3100/8001).
  - home/.env.local, admin/.env.local 의 하드코딩 IP 주석 처리(자동감지 우선).
  → localhost로 열면 localhost, 서버IP로 열면 서버IP로 자동 동작(파일 수정 불필요).
  * 전제: FE·admin·BE가 같은 host에서 서비스될 때. 다른 host면 env로 고정.

● Java 버전: be/pom.xml <java.version> 25 → 17 로 수정(로컬 JDK가 Temurin 17이라
  "release version 25 not supported" 빌드 실패했었음). 서버도 Java 17+ 필요.

● 로컬 작업 전환: application.yml datasource 기본값을 원격→로컬로.
  - DB_HOST 기본 localhost, DB_USER 기본 chin4_test (로컬은 chin4 계정 없음, chin4_test/1234 존재),
    CORS allowed-origin-patterns = localhost,127.0.0.1.
  - 원격 전환 시엔 코드 수정 없이 환경변수만: DB_HOST=175.125.94.198, DB_USER=chin4.
  * ⚠ BE가 원격 DB에 붙으면 ddl-auto=update 스키마 변경 + DataSeeder 계정 시드가 원격에 실행됨(주의).

● [2026-07-23] 서버에서 로그인 시 "서버 연결 실패(백엔드 9001 확인)" 재발 → 원인은 CORS.
  - FE는 host 자동해석으로 http://175.125.94.198:9001 을 정상 호출하나, BE의
    app.cors.allowed-origin-patterns 가 localhost,127.0.0.1 만 허용 → 서버IP Origin
    (http://175.125.94.198:8001) preflight 403 → 브라우저가 응답 차단 → fetch throw →
    login page.tsx catch 블록의 "서버 연결 실패" 메시지 표출(실제론 CORS 거부).
  - 해결: application.yml
      allowed-origin-patterns: ${CORS_ORIGINS:http://localhost:*,http://127.0.0.1:*,http://175.125.94.198:*}
    (localhost 유지 + 서버IP 추가, CORS_ORIGINS 환경변수로 오버라이드 가능)
  - 검증: 서버IP:8001/:3100/localhost:8001 Origin preflight 모두 200 + Access-Control-Allow-Origin 정상.
  - ★ 서버 반영하려면 be 재패키지/재배포(또는 서버 BE 재기동) 필요. 코드에 이미 반영됨.
  - 구분법: 서버 브라우저 개발자도구 Network에서 login 요청이
      · "CORS error"/차단 → 위 문제(이번 수정으로 해결)
      · "connection refused"/무응답 → BE 미기동 또는 9001 포트 미개방(방화벽) 확인.

● 실행: 루트 start-all.ps1 (be/home/admin), stop-all.ps1.
  - 특정만: .\start-all.ps1 be  (be|home|admin)

────────────────────────────────────────────────────────────────
[1] docs/07.txt 작업 (P1~P8) — 매니저·버즈·상품 계약종료일·매니저신청
────────────────────────────────────────────────────────────────
■ P1. DB 스키마 확장 (JPA 엔티티 필드 추가 → ddl-auto 자동 반영)
  - product: contract_end_date(DATE), install_product(BIT, 기본 false)
  - user: manager_status(CHAR1, 기본 N / I신청 / Y승인), manager_code(VARCHAR20),
          manager_center_id(BIGINT), manager_sdate(VARCHAR10), manager_edate(VARCHAR20)
  - 기존 user 행 manager_status NULL → 'N' 백필.

■ P2. 상품등록 UI — 계약(영업)종료일 + 설치상품 체크박스
  - BE: ProductController / PartnerProductController 의 DTO·dto()·apply()에
        contractEndDate(문자열→LocalDate 파싱), installProduct 반영.
  - FE: master/ProductForm.tsx, partner/ProductForm.tsx 에 날짜 입력 + 설치상품 체크박스.

■ P3. 상품마켓 D-day 표시 (버즈/매니저 공용 마켓)
  - BE: BuzzMarketController.card() 에 contractEndDate/installProduct 추가.
  - FE: lib/dday.ts (computeDday: days/label/closingSoon/expired/message).
        MarketSection: 이미지 우상단 D-day 배지(여유=골드, 임박/마감=빨강),
        7일 미만 시 상품명 하단 붉은 "[영업마감일 O일 남았습니다.]".
        ProductDetail: 헤더 D-day 배지 + 임박 안내.

■ P4. 회원가입 sales_center_id 결정 로직
  - 홈 signup(AuthController): ①입력값 → ②추천인(referral_code=추천인 userId)의 sales_center_id 승계
                              → ③주소(시/구/군) center_code 매칭.
  - 버즈admin 회원등록(BuzzMemberController): 로그인 회원의 sales_center_id 승계(없으면 주소매칭).
  * 매니저 신규 등록은 별도 폼 없음(P7 승급신청으로 생성). 센터admin엔 회원등록 화면 없음.

■ P5. 버즈 ↔ 매니저 admin 뷰 전환 (MANAGER 전용)
  - theme.tsx: isManager를 실제 role 고정 → "현재 뷰(view)" 기반으로 변경.
    role/canToggle(=MANAGER)/view/setView 컨텍스트 추가, localStorage(buzz_view_mode) 유지.
    BUZZ는 항상 버즈 뷰 고정.
  - BuzzTopbar: MANAGER만 상단 우측 [매니저admin 보기]/[버즈admin 보기] 토글.
  - MANAGER 로그인은 기존대로 /buzz 진입(가드 BUZZ+MANAGER 허용).

■ P6. 1차영업신청 버튼 + 영업등록 상품 필터 (7일 이상만)
  - lib/dday.ts canApplySale(): 종료일 미등록(상시) 또는 7일 이상 → 신청가능.
  - MarketSection 카드: 버즈 뷰 & 7일이상 → [상세 보기] 옆 [1차영업신청]
    (링크 /buzz/pipeline/new?productId=&categoryId= → 자동선택).
  - ProductDetail: 버즈 뷰 & 7일이상 → 우상단·우하단 [1차영업신청].
  - SaleForm: 쿼리로 카테고리·상품 자동선택(StrictMode 안전 prevCat 방식),
    상품 드롭다운은 7일이상 상품만. pipeline/new 페이지 <Suspense> 래핑(useSearchParams).

■ P7. 매니저 신청하기 (BUZZ 전용)
  - BE: BuzzManagerController(/api/buzz/manager/**)
    GET /status, GET /divisions(본부=center_code center_code IS NULL),
    GET /centers?divisionIdx=(head_code 그룹), POST /apply{divisionIdx,centerId}
    → manager_center_id=centerId, manager_sdate=오늘, manager_status='I' (중복신청 차단).
  - FE: ManagerApplyForm + /buzz/network/manager-apply (지역본부→센터 cascade),
        NetworkSection "내 버즈 네트워크" 우측 [매니저 신청하기](BUZZ,status N) / 상태배지(I·Y).

■ P8. 매니저admin 영업관리 재편
  - BE: BuzzSalesController
    GET /api/buzz/sales/intake  = 내 manager_center_id와 같은 sales_center_id 버즈의 미배정(managerId NULL) 1차영업
    GET /api/buzz/sales/managed = 내가 배정받은(managerId=나) 건
    배정받기 = 기존 /{id}/assign(교육 이수·승인 필수) 재사용.
  - FE: ManagerSalesSection(mode=intake|managed, 소메뉴 탭) + /buzz/intake, /buzz/managed.
        BuzzTopbar 매니저 nav: [관리영업 파이프라인] → [영업관리 ▾] 드롭다운(버즈1차접수현황·2차영업관리).

────────────────────────────────────────────────────────────────
[2] docs/08.txt 작업 (S1~S4) — 공지사항 + 상품등록 캘린더
────────────────────────────────────────────────────────────────
■ S1. 상품등록 계약종료일 지난일자 선택 불가
  - master/partner ProductForm: date input min=오늘 + 제출 시 과거날짜 차단 검증.

■ (추가요청) 고급 캘린더
  - components/DatePicker.tsx 신규: 커스텀 캘린더 팝오버(월이동·요일색·과거일 비활성·
    오늘/지우기 푸터·외부클릭 닫힘), variant "dark"(본사)/"light"(파트너).
  - 두 ProductForm의 네이티브 date input을 DatePicker로 교체.

■ S2. notice 테이블 + BE CRUD
  - notice 테이블(엔티티 Notice): id, title, content(TEXT), target_type(enum DIVISION/CENTER/MANAGER/BUZZ),
    all_flag(전체공지), target_id(본부 or 센터 center_code.idx), published(게시여부), created_at, updated_at.
  - NoticeController(/api/notices, MASTER_ADMIN): 대상별 목록·등록·상세·수정·삭제.
  - MyNoticeController(/api/my/notices, 인증): 로그인 role→대상+소속센터 필터.
    본부/센터=sales_center_id, 매니저=manager_center_id, 버즈=sales_center_id. (전체공지 or target_id 일치)
  - SecurityConfig: /api/notices/** = MASTER_ADMIN, /api/my/** = authenticated.

■ S3. 본사 admin 공지사항 UI
  - nav.ts: [수당 및 정산 관리] 아래 [공지사항] + 서브4(본부/센터/매니저/버즈).
  - lib/noticeMeta.ts(세그먼트↔Target, scope division/center), lib/api.ts apiDelete 추가.
  - NoticeList(대상별 리스트 + 우상단 [등록하기] + 제목클릭→수정),
    NoticeForm(등록/수정/삭제, 전체공지 or 본부선택[division]/본부→센터 cascade[center·manager·buzz], 게시여부).
  - 라우트: /master/notice(→division), [target](리스트)·[target]/new·[target]/[id](수정).

■ S4. 각 역할 admin 공지 조회 (리스트 + 상세 모달)
  - components/MyNoticeView.tsx: /api/my/notices 리스트 + 상세 모달, 테마 토큰 주입식(공용).
  - 버즈/매니저: buzz/sections/NoticeSection + /buzz/notices, BuzzTopbar 두 nav에 공지사항.
  - 본부: division/sections/NoticesSection mock→실데이터 교체, 라벨 "공지사항".
  - 센터: center/sections/NoticeSection + /center/notices, CenterTopbar에 공지사항.

■ (추가요청) 매니저의 버즈 뷰 공지
  - 요구: role=MANAGER라도 버즈admin(버즈 뷰) 공지사항엔 BUZZ 공지가 표시되어야 함.
  - MyNoticeController: /api/my/notices?as=BUZZ 파라미터. 매니저가 as=BUZZ면 소속센터(sales_center_id)
    기준 BUZZ 공지 반환(매니저→BUZZ만 허용, BUZZ→MANAGER 승격은 무시).
  - MyNoticeView asRole prop, buzz NoticeSection: asRole = isManager ? "MANAGER" : "BUZZ".
  → 매니저 버즈 뷰=BUZZ 공지 / 매니저 뷰=MANAGER 공지, 토글 시 즉시 전환.

────────────────────────────────────────────────────────────────
[3] 신규/주요 파일 목록
────────────────────────────────────────────────────────────────
BE (be/src/main/java/com/chinbiz/api/)
  - notice/Notice.java, NoticeRepository.java, NoticeController.java, MyNoticeController.java
  - buzz/BuzzManagerController.java (매니저 승급신청)
  - buzz/BuzzSalesController.java (intake/managed 추가)
  - product/Product.java, user/User.java (필드 추가)
  - config/SecurityConfig.java (notice·my 규칙)

FE (admin/src/)
  - components/DatePicker.tsx (고급 캘린더)
  - components/MyNoticeView.tsx (역할별 공지 리스트+상세)
  - components/master/NoticeList.tsx, NoticeForm.tsx
  - components/buzz/ManagerApplyForm.tsx, sections/NoticeSection.tsx,
    sections/ManagerSalesSection.tsx, sections/MarketSection.tsx(D-day),
    ProductDetail.tsx(D-day+신청), SaleForm.tsx(자동선택+필터), theme.tsx(뷰토글), BuzzTopbar.tsx
  - components/center/sections/NoticeSection.tsx, division/sections/NoticesSection.tsx(실데이터)
  - lib/dday.ts, lib/noticeMeta.ts, lib/api.ts(apiDelete), lib/auth.ts·api.ts(resolveServiceUrl)
  - app/master/notice/**, app/buzz/{notices,intake,managed,network/manager-apply}/**, app/center/notices/**

FE (home/src/)
  - lib/api.ts (resolveServiceUrl), app/login/page.tsx (ADMIN_URL 자동해석)

────────────────────────────────────────────────────────────────
[4] 검증용 데모 데이터 (로컬 chin4 DB에 잔존)
────────────────────────────────────────────────────────────────
  - ddaytester / testpass1234  (BUZZ, 소속센터 21 강남)
  - p5manager  / testpass1234  (MANAGER, 관리센터 21) + 상품2 교육 이수·승인
  - 상품 1,2 계약종료일: 상품1 D-3(임박), 상품2 D-30(여유)
  - notice: 본부/센터/매니저/버즈 대상 전체공지 + 강남(21)·강동(22) 특정센터 공지 여러 건
  * 데모/재검증용. 정리 원하면 삭제 가능.

────────────────────────────────────────────────────────────────
[5] 참고/주의
────────────────────────────────────────────────────────────────
  - curl로 한글 body 전송 시 Git Bash 인코딩 문제로 400 발생 가능 → JSON 파일 --data-binary 사용.
    (FE fetch는 UTF-8이라 정상)
  - Next dev(admin) 오래 실행 시 새 라우트 404/RSC prefetch 폴백 로그 발생 → dev 서버 재시작으로 해소.
  - 비밀번호는 BCrypt 해시 저장(평문 금지). 그 외 개인정보는 평문(요구사항).
  - 금액 정수(원), 비율 상품별 설정값 사용, 정산 원장 Insert-only 원칙 유지.

====================================================================
[추가] docs/09.txt 작업 — 대시보드 DB 연동 + 파트너 상담신청 (2026-07-23)
====================================================================

(A) 본사 파트너사관리 [파트너관리]/[상담신청] 서브메뉴
  - home 랜딩 STRATEGIC INQUIRY 카드: 단계선택 후 [회사명·담당자·연락처·이메일·메시지]
    입력 폼 → POST /api/public/partner-inquiry (공개). 접수 성공 시 완료 메시지.
    (home/src/components/landing/PartnerInquiry.tsx)
  - 신규 테이블 partner_inquiry (status NEW/DONE/CANCELED). PartnerInquiry 엔티티/Repo/Controller.
  - 본사 LNB '파트너사 관리'에 서브메뉴 [파트너관리](기존 리스트)/[상담신청] 추가 (nav.ts).
  - /master/partners/inquiries: 접수목록 + [상담완료]/[신청취소] 버튼
    → POST /api/partners/inquiries/{id}/status (MASTER_ADMIN 전용).

(B/C) 버즈·매니저 대시보드 CP/MP DB 연동
  - GET /api/buzz/dashboard?as=buzz|manager (BuzzDashboardController)
    · as=buzz  → 회원구분 BUZZ+TOPBUZZ, member_id=본인, 이달, CP=CP_READY MP=MP_CONFIRMED 합
    · as=manager→ 회원구분 MANAGER
  - WalletSection.tsx: 뷰(버즈/매니저)에 따라 as 파라미터 전환. "(즉시 출금 가능)" 문구 제거.

(D) 센터 대시보드 CP/MP + 버즈/매니저 분리
  - GET /api/center/dashboard (CenterManagerController): BUZZ_CENTER+MANAGER_CENTER 합산 +
    버즈몫(cpBuzz/mpBuzz)·매니저몫(cpManager/mpManager) 분리. member_id=로그인 센터.
  - center/sections/WalletSection.tsx 실연동. "즉시 출금 가능한" → "출금 가능한".

(E) 본부 대시보드 CP/MP
  - GET /api/division/dashboard (DivisionDashboardController): 회원구분 DIVISION, member_id=본인.
  - division/sections/WalletSection.tsx 실연동. "즉시" 제거.

(F) 본사 대시보드 금월 GMV / 누적 확정수익
  - GET /api/org/dashboard (MasterDashboardController):
    · gmv    = 이달 sale×product 판매가 합 (SaleRepository.sumGmvBetween, theta join)
    · profit = 회원구분 HQ, MP_CONFIRMED 합 (member_id=MASTER_ADMIN)
    · 각각 전월 대비 증감율(gmvRate/profitRate). 전월 0 → (당월>0?100:0).
  - components/master/MasterKpi.tsx (신규, 클라이언트) → master/page.tsx 하드코딩 KPI 2카드 대체.

★ 회원구분(member_type) 매핑 주의 (docs/09.txt 표기와 실제 구현 차이)
  - docs/09.txt에는 "본부=HQ, 본사=MASTER"로 적혀 있으나, 실제 Allowance 원장이 사용하는 값은
    본부=DIVISION(레코드 member_id=본부 admin), 본사=HQ(레코드 member_id=MASTER_ADMIN)이며
    MASTER 구분 레코드는 생성되지 않음. → 본부 대시보드=DIVISION, 본사 확정수익=HQ 로 구현함.
    (7단계 매트릭스 §3의 6.본부/7.본사 정의와 일치)

검증 (2026-07-23, 로컬 chin4)
  - 입점제안 접수(201) → 본사 목록 조회(1건) → 상담완료(DONE) 처리 OK.
  - buzz(as buzz/manager)/center/division/org dashboard 모두 200 + JSON(cp/mp, gmv/profit) 정상.
  - 권한: 각 /api/{buzz,center,division}/**·/api/partners/** 타역할·무토큰 403.
  - admin(3100) 화면: /master/partners/inquiries 렌더, /master KPI 카드 GMV ₩27,920,000·확정 ₩5,000
    (DB값) 표시, 콘솔 에러 0. 검증 후 테스트 데이터 삭제.
  - db/schema_chin4.sql 에 partner_inquiry 테이블 반영.

====================================================================
[추가] docs/10.txt 작업 — 4개 admin 메뉴 재구성 (2026-07-23)
====================================================================

■ 매니저 admin (buzz 매니저 뷰)
  M1) [관리마켓·배정]→[관리마켓]. 페이지 헤더 뷰별 전환(MarketHead.tsx):
      매니저=「관리마켓 / 교육이수 및 2차영업 가능 상품」, 버즈=기존 유지.
      MarketSection 카드 sub("총 N개…")는 매니저 뷰에서 제거.
  M2) 매니저 nav에서 [네트워크] 삭제(BuzzTopbar NAV_MANAGER). /buzz/network는 버즈 전용으로
      BuzzShell BUZZ_ONLY에 추가(매니저 접근 시 대시보드로 리다이렉트).
  M3) 교육관리: 이수완료(completed) 상품은 목록에서 제외(BuzzEducationController.list 필터).
      [교육완료] 클릭 → 자동배정 동의 레이어("…자동 배정 여부를 허용하시겠습니까?" [미동의][동의])
      → POST /api/buzz/education/complete {productId, autoAssign} → education.auto_assign 저장.
      · Education 엔티티에 auto_assign(boolean) 컬럼 추가.

■ 센터 admin
  C1) [소속버즈관리]→[소속버즈] + 소메뉴 [소속버즈회원](/center/buzz)·[1차영업관리](/center/buzz/sales)
      · 소속버즈회원: GET /api/center/buzz-members (sales_center_id=내 센터 BUZZ, 가입일/아이디/명/영업수/완료수)
      · 1차영업관리: GET /api/center/sales/buzz (소속 버즈 1차영업 상품별 합산)
  C2) [소속매니저관리]→[소속매니저] + 소메뉴 [2차영업관리](/center/managers/sales)
      · GET /api/center/sales/manager (manager_center_id=내 센터 매니저의 2차영업 상품별 합산)
  C3) [상품 및 교육 컨트롤] 메뉴/라우트(app/center/products) 삭제.
  C4) [정산 원장] 소메뉴 [버즈회원 영업 정산현황](/center/settlement)·[관리매니저 영업 정산현황](/…/manager)
      ·[수당지급 현황](/…/payouts) → GET /api/center/settlement?scope=buzz|manager|payouts
      (BUZZ_CENTER/MANAGER_CENTER 원장, payouts=MP 확정. member_id=센터 admin userId)
  C5) 대시보드 [센터 MP 확정수당 출금] 섹션 삭제(center WalletSection).
  · 신규 BE: CenterSalesController, CenterSettlementController. 신규 FE 섹션:
    BuzzMembersSection, SalesAggSection(scope buzz/manager), CenterSettlementSection(scope 3종).

■ 본부 admin
  D1) 대시보드 [2차 영업(매니저관리) 본부 배정액] 박스 + [본부 MP 확정수당 출금] 섹션 삭제(division WalletSection).
  D2) [광역 파이프라인]→[센터 영업관리](라벨+페이지 헤더).
  D3) [수당 정산원장]→[정산현황] + 소메뉴 [수당지급현황](/division/settlement/payouts)·[정산원장](/division/settlement)
      → GET /api/division/settlement?scope=ledger|payouts (DIVISION 원장, member_id=본부 admin userId).
  · 신규 BE: DivisionSettlementController. FE: DivisionSettlementSection, DivisionTopbar 드롭다운 지원 추가.

■ 파트너 admin
  P1) [직영 매니저관리] 메뉴/라우트(app/partner/managers) 삭제.
  P2) [정산/수당 현황] 소메뉴 [정산 현황](/partner)·[정산 원장](/partner/ledger)
      → GET /api/partner/settlement/ledger (partner_id=내 파트너의 7주체 전체 전표).
  · 신규 BE: PartnerSettlementController. FE: LedgerSection, PartnerTopbar 드롭다운 지원 추가.

■ 공통/토폴로지
  · 드롭다운 childActive 헬퍼: 부모와 href 같은 인덱스 자식은 정확일치, 그 외 prefix 일치(오활성 방지).
  · SecurityConfig 기존 규칙으로 신규 엔드포인트 커버(/api/center|division|partner/** + buzz).

검증 (2026-07-23, 로컬 chin4)
  · BE compile OK, 재기동 후 전 엔드포인트 200. education.auto_assign 컬럼 ddl-auto 생성 확인.
  · 매니저 뷰: 관리마켓 헤더/카드 sub 제거·네트워크 메뉴 제거 확인. 교육 [교육완료]→동의 모달→저장
    →목록에서 제외, DB completed=1/auto_assign=1 확인.
  · 센터: 소속버즈회원 3명(영업/완료수)·1차영업관리 상품합산(활동2·접수3·완료2·건당₩3,000) 실데이터 렌더.
    (데모 center 계정 sales_center_id NULL→21로 지정해 데이터 확인)
  · 본부 대시보드 2차박스·출금 제거 확인, 수당지급현황 렌더. 파트너 정산원장 렌더(파트너13에 29전표 존재).
  · 콘솔 에러 0. db/schema_chin4.sql education.auto_assign 반영.

★ 참고: 구 센터 컴포넌트(BuzzSection/ProductsSection/SettlementSection - 센터 mock)는 미사용 처리(라우트 미연결).

  [보완 2026-07-23] 관리마켓(매니저 뷰) 추가 요청
   1) 교육이수 완료 상품만 노출: /api/buzz/products?as=manager → 로그인 매니저 education.completed=true
      상품만 spec 필터 + 각 카드에 autoAssign 포함(BuzzMarketController).
   2) 검색필드 위 카드 sub타이틀 "관리 마켓" 삭제: 매니저 뷰에서 Card title 미표시(BuzzUI.Card title optional).
   3) [상세 보기] 옆 자동배정 [동의]/[미동의] 토글 버튼 → 클릭 즉시 저장
      POST /api/buzz/products/{id}/auto-assign {autoAssign} (이수완료 상품만 허용) → education.auto_assign 갱신.
   · FE MarketSection: isManager면 as=manager 조회 + 낙관적 토글. 뷰 전환/페이징 경합으로 옛 응답이
     최신 응답을 덮어쓰던 레이스 → reqRef 요청토큰 가드로 최신 요청만 반영(수정).
   · 검증: 매니저 마켓 교육완료 2건만 노출, 토글 클릭→라벨 즉시 전환 + DB auto_assign 반영, 콘솔 에러 0.

  [보완 2026-07-23#2] 파트너 정산원장 재정의 + 센터 링크 + 드롭다운 버그
   · 파트너 [정산/수당현황]-[정산원장]: 기존 7주체 Allowance 전표 → sale.status='구매확정' &
     product.partner_id=내 파트너 인 판매건 목록으로 변경. 각 행 상품 total_allowance(총수당/위탁비) 표시,
     상단에 건수·총수당 합계. (PartnerSettlementController.ledger 재작성, FE LedgerSection 재작성)
     검증: partner(id14) 구매확정 1건 total_allowance ₩400,000 렌더.
   · 센터 대시보드 [소속 버즈회원별 정산 리포트]→/center/settlement(버즈회원 영업 정산현황),
     [소속 관리매니저별 정산 리포트]→/center/settlement/manager(관리매니저 영업 정산현황) 링크 연결.
   · 센터/본부 상단 드롭다운 메뉴 hover 시 위치 튐 버그 수정: 드롭다운 패널이 헤더토큰(ct/dv.header)의
     "sticky top-0"을 상속받아 position:sticky로 계산되던 문제 → 패널 전용 클래스(불투명 bg+border, sticky 제거)로
     교체. 래퍼를 flex h-16 items-center로 만들어 top-full=헤더 하단(서브메뉴가 상단바 아래로 노출).
     파트너 드롭다운도 동일 패턴으로 통일. (opacity 트랜지션, whitespace-nowrap)

  [보완 2026-07-23#3] 관리마켓 자동배정 UI → 토글 스위치(button.png)
   · 기존 [자동배정 동의/미동의] 텍스트 버튼 → iOS식 토글 스위치(파란 track+흰 knob, ON=우/blue, OFF=좌/gray)
     + "자동배정" 라벨. 클릭 즉시 저장(기존 /api/buzz/products/{id}/auto-assign 재사용).
   · knob 위치는 인라인 style transform(translateX 2/22px) 사용 — Tailwind v4 arbitrary translate/left
     클래스가 Turbopack dev HMR에서 값이 안 먹는 이슈 회피. MarketSection.tsx.
   · 검증: 새로고침 시 OFF=좌/gray, ON=우/blue 일치. 클릭→React inline style/track class/aria 갱신 +
     DB auto_assign 반영. (dev 비-compositing 프리뷰는 CSS 트랜지션 중 computedStyle이 지연되나 실제 DOM은 정상)

====================================================================
[DB 마이그레이션] 서버 DB 신규 필드/테이블 적용 쿼리  ★상시 관리★
====================================================================
※ 규칙: 앞으로 DB에 필드/테이블이 추가될 때마다, 서버(chin4)에 적용할
   ALTER/CREATE 쿼리를 아래에 계속 누적 기재한다. (신규 항목은 맨 아래에 append)
   - 전체 멱등 스크립트: db/migration_new_fields.sql (이미 있으면 skip, 재실행 안전)
     실행: mysql -u root -p chin4 < db\migration_new_fields.sql
   - BE는 ddl-auto=update 로 자동 반영되지만, 수동 적용/문서화용으로 병행.
   - MySQL8은 ADD COLUMN IF NOT EXISTS 미지원 → 이미 있으면 "Duplicate column" 에러(무시 가능).
     안전하게는 위 멱등 스크립트 사용 권장.

── [신규 테이블] ───────────────────────────────────────────────
-- 수당 원장(7주체 정산) : db/migration_new_fields.sql 의 CREATE TABLE `allowance` 참조
-- 파트너 입점 상담신청   : db/migration_new_fields.sql 의 CREATE TABLE `partner_inquiry` 참조
-- 웹푸시 구독(모바일 PWA) : db/migration_new_fields.sql 의 CREATE TABLE `push_subscription` 참조 (2026-08-01)
--   CREATE TABLE `push_subscription` (id, account, endpoint UNIQUE, p256dh, auth, created_at)
--   ddl-auto 자동 생성되나 배포용 병행. PWA 알림권한 허용 시 브라우저 구독을 로그인 계정과 저장.
-- 본사 RBAC 담당자 지정 : db/migration_new_fields.sql 의 CREATE TABLE `admin_scope` 참조 (2026-08-07 docs/20)
--   CREATE TABLE `admin_scope` (login_id PK, areas VARCHAR)  -- areas=CSV(A,B,C,D). 행 없음=슈퍼(전체 접근).
--   ddl-auto 자동 생성되나 배포용 병행. MASTER_ADMIN 계정별 접근 메뉴영역 지정.

── [컬럼 추가] 서버 즉시 적용용(개별 ALTER) ─────────────────────
-- product (계약종료일/설치상품/키워드배지)
ALTER TABLE `product` ADD COLUMN `contract_end_date` date DEFAULT NULL;
ALTER TABLE `product` ADD COLUMN `install_product` bit(1) NOT NULL DEFAULT b'0';
ALTER TABLE `product` ADD COLUMN `kw_popular` bit(1) NOT NULL DEFAULT b'0';
ALTER TABLE `product` ADD COLUMN `kw_recommended` bit(1) NOT NULL DEFAULT b'0';
-- sale (주문번호/소속센터대표/매니저배정일시/고객지역센터)
ALTER TABLE `sale` ADD COLUMN `order_no` varchar(60) DEFAULT NULL;
ALTER TABLE `sale` ADD COLUMN `parent_id` bigint DEFAULT NULL;
ALTER TABLE `sale` ADD COLUMN `manager_dated_at` datetime(6) DEFAULT NULL;
ALTER TABLE `sale` ADD COLUMN `customer_center_id` bigint DEFAULT NULL;
-- user (소속센터/계좌/상태/매니저 승급)
ALTER TABLE `user` ADD COLUMN `sales_center_id` bigint DEFAULT NULL;
ALTER TABLE `user` ADD COLUMN `account_holder` varchar(50) DEFAULT NULL;
ALTER TABLE `user` ADD COLUMN `account_number` varchar(40) DEFAULT NULL;
ALTER TABLE `user` ADD COLUMN `bank_name` varchar(40) DEFAULT NULL;
ALTER TABLE `user` ADD COLUMN `status` varchar(20) DEFAULT NULL;
ALTER TABLE `user` ADD COLUMN `manager_center_id` bigint DEFAULT NULL;
ALTER TABLE `user` ADD COLUMN `manager_code` varchar(20) DEFAULT NULL;
ALTER TABLE `user` ADD COLUMN `manager_sdate` varchar(10) DEFAULT NULL;
ALTER TABLE `user` ADD COLUMN `manager_edate` varchar(20) DEFAULT NULL;
ALTER TABLE `user` ADD COLUMN `manager_status` varchar(1) DEFAULT NULL;
-- education (자동배정 동의여부; docs/10)
ALTER TABLE `education` ADD COLUMN `auto_assign` bit(1) NOT NULL DEFAULT b'0';
-- allowance (확정일자/확정월; 2026-07-24) ★최근 추가
ALTER TABLE `allowance` ADD COLUMN `fixed_date` datetime(6) DEFAULT NULL;
ALTER TABLE `allowance` ADD COLUMN `fixed_month` varchar(6) DEFAULT NULL;
-- allowance_payment (정산/수당지급 테이블 신규; 2026-07-24) ★최근 추가
CREATE TABLE IF NOT EXISTS `allowance_payment` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `member_id` varchar(60) DEFAULT NULL,
  `member_type` enum('BUZZ','BUZZ_CENTER','DIVISION','HQ','MANAGER','MANAGER_CENTER','MASTER','TOPBUZZ') DEFAULT NULL,
  `fixed_month` varchar(6) DEFAULT NULL,
  `payment_amount` bigint DEFAULT NULL,
  `created_date` datetime(6) DEFAULT NULL,
  `payment_flag` char(1) DEFAULT 'N',
  `payment_date` datetime(6) DEFAULT NULL,
  `account_holder` varchar(50) DEFAULT NULL,
  `account_number` varchar(40) DEFAULT NULL,
  `account_bankname` varchar(40) DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- product (단순배송상품/취소보전비/취소비용; docs/11) ★최근 추가
ALTER TABLE `product` ADD COLUMN `simple_delivery` bit(1) NOT NULL DEFAULT b'0';
ALTER TABLE `product` ADD COLUMN `cancel_fee_flag` bit(1) NOT NULL DEFAULT b'0';
ALTER TABLE `product` ADD COLUMN `cancel_amount` bigint DEFAULT 0;

-- terms (약관/동의서 테이블 신규; 2026-07-27 docs/15) ★최근 추가
--   본사 [시스템설정]-[약관설정]에서 10종 약관 등록·수정. home/버즈admin 노출.
CREATE TABLE IF NOT EXISTS `terms` (
  `code` varchar(40) NOT NULL,
  `title` varchar(120) NOT NULL,
  `content` longtext,
  `sort_order` int DEFAULT NULL,
  `updated_at` datetime(6) DEFAULT NULL,
  PRIMARY KEY (`code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- (이후 신규 필드는 여기 아래에 날짜/사유와 함께 계속 추가)
-- [2026-08-30] sale: 현장설치 사진 (docs/25_2, 매니저 2차영업관리 영업권확보 레이어 업로드)
ALTER TABLE `sale` ADD COLUMN `install_photos` text;

  [보완 2026-07-24] 센터 1차/2차영업관리 개별 내역 + 고객정보 마스킹
   · 센터 [1차영업관리]/[2차영업관리]에 기존 상품별 합산은 유지하고, 개별 영업 내역 리스트를 추가.
     - 1차: 소속 버즈(sales_center_id) 개별 1차 영업신청 내역
     - 2차: 소속 매니저(manager_center_id) 개별 2차 영업 진행내역
   · 고객정보 서버 마스킹(원문 PII 미전송): com.chinbiz.api.common.Mask
     - 이름: 홍길동→홍O동 (첫/끝 유지, 가운데 O)
     - 전화: 010-1234-5678→010-XXXX-5678 (가운데 그룹 X)
     - 이메일: honggildong@naver.com→hongxxxxx@naver.com (로컬 앞4자 + xxxxx + 도메인)
   · BE: CenterSalesController + GET /api/center/sales/{buzz|manager}/list (masked). 고객명=담당자명(없으면 대표자명).
   · FE: components/center/sections/SalesListSection.tsx(scope buzz|manager) → 두 페이지 하단에 추가.
   · 테스트 데이터(2차용, 삭제하지 않음): sale에 manager_id=37(p5manager,센터21) 2건 INSERT
     (order_no OD-TEST2C-1/2, 이순신/강감찬). 1차는 기존 6건으로 확인.
   · 검증: 1차/2차 화면 모두 합산+개별내역 렌더, 마스킹 정상(강O찬/010-XXXX-2222/kangxxxxx@daum.net), 콘솔 에러 0.

  [보완 2026-07-24] 모든 admin 상단바 소매뉴 hover 깜빡임 → 클릭 토글로 변경
   · 증상: 소매뉴 있는 상단 메뉴에 마우스 올리면 서브메뉴가 나타났다 사라져 클릭이 어려움(hover 방식).
   · 해결: 4개 상단바(Buzz/Center/Division/Partner)를 hover(group-hover) → **클릭 토글**로 전환.
     - 부모 클릭 시 열림/닫힘, 서브메뉴 링크 클릭 시 이동+닫힘, 외부 클릭·라우트 변경 시 닫힘.
     - openKey(useState) + navRef(외부클릭 mousedown 감지) + pathname 변경 시 닫힘.
     - 화살표 ▼(닫힘)/▲(열림) 표시. 패널은 불투명 배경으로 완전 표시(사라짐 없음).
   · 본사(master) admin은 좌측 세로 LNB(인라인 확장, hover 아님)라 변경 불필요.
   · 파일: components/buzz/BuzzTopbar.tsx, center/CenterTopbar.tsx, division/DivisionTopbar.tsx, partner/PartnerTopbar.tsx
   · 검증: 센터 [정산 원장] 클릭→서브 3개 유지→[관리매니저 영업 정산현황] 클릭→해당 페이지 로드, 콘솔 에러 0.
   ※ 공유 컴포넌트(상단바) 수정 후 Turbopack dev가 중첩 라우트를 드롭(404)하는 현상 재발 →
     admin에서 .next 삭제 후 npm run dev 재기동으로 복구(서버 빌드에선 무관). 기존과 동일 이슈.

====================================================================
[추가] docs/11.txt 작업 — 상품필드/취소보전/버즈·매니저 수당정산 (2026-07-25)
====================================================================
■ P1. 본사 상품등록 필드 추가 (product)
  - simple_delivery(단순배송상품), cancel_fee_flag(취소 시 매니저 보전비 있음), cancel_amount(취소비용)
  - Product 엔티티 + ProductController/PartnerProductController DTO·dto·apply + master ProductForm UI
    (보전비 체크 시 취소비용 입력 노출). 검증: 상품 dto cancelFeeFlag/cancelAmount 왕복.
■ P2. 파트너 취소 프로세스 변경 (AllowanceService.cancelOrder)
  - 매니저 보전비 조건: [설치상품] → [매니저보전비 있음(cancel_fee_flag)], 금액 50000 → product.cancel_amount.
  - 검증: product8(cff=1,amount=30000,install=0) 배송/설치 sale 파트너취소 → 매니저 CANCEL_FEE ₩30,000 MP (유지).
■ P3. 본부 대시보드 정리 (division WalletSection)
  - [기여도 요약] 카드 삭제, [산하 센터별 본부 수당 기여도 랭킹 보기] 버튼을 현황판 카드 아래로 이동.
■ P4/P5. 버즈/매니저 [수당/정산현황] + 대시보드 수당구성
  - 상단바 첫 메뉴 → [수당/정산현황] 드롭다운(수당현황/정산현황), 대시보드는 로고 링크(기존 유지).
  - MP 확정수당 카드: 출금 버튼 삭제 + "확정된 MP수당은 익월 정산됩니다." 문구.
  - 수당현황: GET /api/buzz/allowances?as=buzz|manager&gubun=&status=&month= (allowance, member_id=본인,
    구분 BUZZ/TOPBUZZ/MANAGER, 상태 CP/MP, 대상월=fixed_month 없으면 created_at월). 검색영역 + 리스트
    (영업일자/구분/상품명/고객명/추천(1차)버즈회원/확정일자/수당금액/상태).
  - 정산현황: GET /api/buzz/payments?as=&month= (allowance_payment). (대상월/구분/정산금액/지급일자).
  - 대시보드 수당구성 4항목 DB(/api/buzz/dashboard 확장: directCp/referralOrPenalty/confirmedMp/cumulativeMp)
    + 클릭 시 수당현황 필터 링크. 매니저=관리CP/패널티(CANCEL_FEE)·보전/확정MP/누적MP.
  - confirmOrder(구매확정) 시 allowance.fixed_date/fixed_month(YYYYMM) 기록.
  - 매니저 [관리마켓]·[교육관리]: 단순배송상품(simple_delivery) 제외(BuzzMarketController as=manager, BuzzEducationController).
  - 신규 FE: buzz sections/AllowancesSection·PaymentsSection + app/buzz/{allowances,payments}. BE: BuzzSettlementController.
  - 검증: 버즈/매니저 대시보드 타일·MP문구, 수당현황(필터 링크 진입), 정산현황, 마켓/교육 simple 제외 모두 실증. 콘솔 에러 0.

★ DB: product 3컬럼 + allowance.fixed_date/fixed_month + allowance_payment 는
   db/migration_new_fields.sql / schema_chin4.sql / 위 [DB 마이그레이션] 섹션 반영됨.
★ 테스트 데이터(삭제 안 함): product8 cancel_fee_flag=1/cancel_amount=30000, sale OD-CANCELTEST-1(취소/반품),
   allowance_payment 2건(buzzgn1 BUZZ 270000·202606 / p5manager MANAGER 150000·202606).
★ 데모 계정 비번 정렬(비-시드 드리프트): buzzgn1·p5manager → sample1234!.

====================================================================
[추가] docs/12.txt 작업 — 마감/정산 워크플로우·추천tree 일괄변경 등 (2026-07-25)
====================================================================
■ P1. 파트너 [구매확정] 시 fixed_date/fixed_month 미기록 (AllowanceService.confirmOrder)
  - docs/11에서 confirm 시 기록하던 것을 제거. fixed_date/fixed_month는 본사 [마감완료] 시점에만 기록.
■ P2. 버즈/매니저 상품상세 상품설명 안내문 (ProductDetail)
  - install_product → "본 제품은 설치 및 교육대상 상품입니다."
  - simple_delivery → "본 제품은 배송상품이며, 설치나 교육이 진행되지 않습니다."
  - cancel_fee_flag → "설치후 주문취소시 영업보전비 {cancel_amount}원 지급되는 상품입니다."
  - BuzzMarketController.card()에 simpleDelivery/cancelFeeFlag/cancelAmount 추가.
■ P3-1. 본사 추천 tree 소속센터 일괄변경 (OrgController + OrgMemberForm)
  - POST /api/org/members/{id}/cascade-center {salesCenterId}: 대상 회원 + referral_code 그래프 하위
    추천 체인(BFS, 사이클 방지) 전체의 sales_center_id 일괄 변경.
  - 회원수정 화면 [하위추천회원 소속센터 전체변경] 버튼 → 본부→센터 선택 모달 → [일괄변경].
  - 검증: buzzgn2(21)→가평군(78) 시 buzzgn2+buzzgn21+test0001(손자)+test002 4명 일괄 변경.
■ P3-2. 본사 [수당및정산관리] 소메뉴 4종 (MasterSettlementController /api/org/settlement, nav.ts, MasterSettlement.tsx)
  - 매출현황: MP·미지급·fixed_month=null·confirm_date=검색월 (+sale join). [마감완료]→fixed_date/fixed_month 기록.
  - 마감내역: fixed_month=검색월·미지급 회원별 합산. [정산완료]→paid=1 + allowance_payment 생성(회원별).
  - 정산내역: allowance_payment fixed_month=검색월·payment_flag=N (user 계좌 join). [지급완료]→flag=Y, payment_date.
  - 지급내역: allowance_payment fixed_month=검색월·payment_flag=Y.
  - 리스트 페이징 없이 스크롤(max-h + overflow-auto, sticky header). /master/settlement→/sales redirect.
  - 검증: 202607 전체 흐름 실증(매출8건→마감→합산→정산완료 전표8건→지급완료→지급내역). 콘솔 에러 0.
★ 테스트 데이터(삭제 안 함): 202607 마감/정산/지급 처리결과(allowance fixed/paid, allowance_payment 8건 지급완료),
   buzzgn2 추천체인 4명 소속센터=78(가평군).

====================================================================
[추가] docs/13.txt 작업 — 홈페이지 상품리스트·입점신청·프로세스·소개 (2026-07-25)
====================================================================
1. 상품리스트 페이지 (/products) + 홈 MAIN SHOWCASE 우측 [전체상품보기] 링크
   - BE PublicProductController 확장: GET /api/public/categories(대분류 LARGE),
     GET /api/public/products/list?keyword=&categoryId=&sort=latest|popular&page=&size=
     (categoryId는 하위 카테고리 재귀 포함, parent_id 그래프). ★가드레일 유지: 단가/마진/수당 미노출.
   - FE: components/landing/ProductBrowse.tsx(검색+대분류칩+정렬(오른쪽)+카드그리드+페이징),
     app/products/page.tsx(히어로+Header/Footer). 카드는 "로그인 후 상세 정보를 확인하세요" 가드레일.
   - [친비즈 상품 제안하기]/하단 CTA는 지시대로 미작업.
2. 파트너사 입점신청 (/partner-apply) — 하단 [회원]-[파트너사 입점문의] 링크
   - 히어로+3특징 / 4단계 입점 프로세스 / 신청폼(1.기업·담당자 / 2.제안상품 + 파일첨부 + 개인정보 동의 모달).
   - 제출 → 기존 POST /api/public/partner-inquiry (본사 [상담신청] 유입). 상세필드는 message로 결합.
   - 파일첨부는 파일명만 message에 기록(실제 업로드는 범위 외). 마지막 [참고---] 섹션은 지시대로 미작업.
3. 업무프로세스 (/process) — 하단 [서비스]-[업무프로세스]
   - 히어로 + E2E 4단계(리드발굴·현장케어·이행결제검증·MP정산) + 주체별 상세 탭(버즈/매니저/파트너 각 4단계 표).
     components/landing/ProcessTabs.tsx.
4. 친비즈소개 (/about) — 하단 [서비스]-[친비즈소개]
   - 히어로+3주체(버즈/매니저/파트너) + 핵심가치 3(초기비용0/전문분업화/정기수익) + 2차 관리매니저 시스템.
   - Footer 링크: 친비즈소개→/about, 상품둘러보기→/products, 업무프로세스→/process, 파트너사입점문의→/partner-apply.
   * GNB(헤더) 링크는 지시 범위(하단=footer)에 따라 기존 해시 스크롤 유지.
   검증: /products(검색·카테고리 10→5·정렬·페이징·가드레일), /partner-apply·/process·/about 렌더, 콘솔 에러 0.
   docx 목업이 수당/단가를 노출하나 프로젝트 가드레일(CLAUDE.md §11·12) 우선하여 비로그인 노출 금지 준수.

====================================================================
[추가] docs/15.txt 작업 — 본사 [약관설정] + 약관 10종 노출 (2026-07-27)
====================================================================
■ 개요: 본사 [시스템설정]-[약관설정]에서 10종 약관/동의서를 원본파일 기반으로 등록·수정하고,
   지정된 지점(home/버즈admin)에 노출. 약관 code/제목:
   1 TOTAL 통합이용약관 / 2 PRIVACY 개인정보처리방침 / 3 BUZZ 버즈회원 이용약관 /
   4 MANAGER 관리매니저 이용약관 / 5 CENTER 센터 이용약관 / 6 DIVISION 본부 이용약관(파일없음·빈값) /
   7 CENTER_PAPER 센터 위수탁계약서 / 8 DIVISION_PAPER 본부 위수탁계약서 / 9 PARTNER 파트너 이용약관 /
   10 PRIVACY_CONSENT 개인정보 수집·이용 동의.
■ BE(be, com.chinbiz.api.terms): Term 엔티티(code PK, title, content LONGTEXT, sortOrder, updatedAt),
   TermRepository, TermController(GET /api/public/terms/{code} 공개 / GET·PUT /api/org/terms[/{code}] MASTER),
   TermSeeder(@Order(20): 10종 code+제목 빈content 시딩). content는 평문 \n 단락 저장.
   ※ 6 DIVISION은 원본 파일 없음 → 빈 content로 유지(등록기능만).
■ 콘텐츠 적재: docs/agree_*.docx(9종) 텍스트 추출 → PUT /api/org/terms/{code}로 적재(관리자 로그인 후).
■ admin(3100): nav.ts 시스템설정 하위 [환경설정]/[약관설정] 서브메뉴, /master/settings/terms
   (components/master/TermsManager.tsx: 좌 10종 목록·미등록표시 + 우 제목/내용 편집·저장).
■ home(8001) 노출:
   - components/site/TermView.tsx(공개 조회 렌더) + app/terms/[code]/page.tsx(전체 페이지) +
     components/site/TermModal.tsx(보기 모달, onAgree 옵션).
   - Footer [법적고지] 이용약관→/terms/total(TOTAL), 개인정보처리방침→/terms/privacy(PRIVACY).
   - 회원가입(signup): (필수)이용약관 [보기]→BUZZ 모달, (필수)개인정보 수집·이용 [보기]→PRIVACY_CONSENT 모달.
   - 파트너 입점신청(PartnerApply): 하드코딩 개인정보 동의서 모달 → TermModal(PRIVACY_CONSENT)로 교체, [동의] 버튼.
■ 버즈admin(3100): ManagerApplyForm(매니저신청) — MANAGER 약관 스크롤 박스 + 동의 체크(미동의 시 신청 불가).
■ DB: terms 테이블 신규 → migration_new_fields.sql / schema_chin4.sql / 위 [DB 마이그레이션] 섹션 반영.
   ⚠ 현재 세션 BE는 원격 서버 DB(175.125.94.198)에 연결되어 있어 terms 행/내용도 원격 DB에 적재됨.

  [보완 2026-07-27] 약관설정 에디터 적용 (요청: editor기능 + HTML코드 등록)
   · admin TermsManager: 기존 plain textarea → 기존 RichTextEditor(서식툴바 B/I/U/S·H2/H3·목록·링크·이미지) 재사용
     + [서식 편집]/[HTML 코드] 모드 토글. HTML 코드 탭에서 태그 직접 작성/수정 가능.
   · 저장 형식이 HTML로 전환됨. 기존 평문(\n)은 pick 시 toHtml()로 <br> 변환(편집 시 자연 마이그레이션).
   · 렌더 전환(평문/HTML 자동판별 looksHtml): home TermView(+terms 페이지/모달), 버즈admin ManagerApplyForm(MANAGER).
     - home globals.css에 .term-content(h1~h4/p/ul·ol/li/a/strong/hr/img/table) 스타일 추가(Tailwind preflight 상쇄).
     - admin은 기존 .rte-content 스타일 재사용(앱 전역).
   · 검증: 약관설정 툴바+모드토글 렌더, 평문→<br> 자동변환, HTML코드 탭 소스편집, DIVISION에 HTML PUT→home /terms/division
     h2 20px/800·ul disc 정상 렌더 확인 후 원복. 콘솔 에러 0.

  [보완 2026-07-27b] 상품설명 에디터 HTML 코드 등록 (본사·파트너 상품등록)
   · RichTextEditor에 [</> HTML] 토글 내장: 서식(WYSIWYG) ↔ HTML 코드(textarea) 전환. 코드 탭에서 태그 직접 작성,
     서식 탭 복귀 시 편집된 HTML을 contentEditable에 재주입(useEffect [htmlMode]).
   · 본사 master/ProductForm(dark)·파트너 partner/ProductForm(light)은 컴포넌트 공유로 자동 적용(코드 수정 불필요).
   · TermsManager는 자체 토글 제거하고 내장 토글 사용(중복 제거, key={sel}로 약관 전환 시 재마운트).
   · 검증: master(dark)/partner(light) 상품등록 [</> HTML] 표시, HTML 입력→서식복귀 시 h2 20px/800·ul disc 렌더,
     light 코드영역 흰 배경. 콘솔 에러 0.

  [보완 2026-07-28] 본사 대시보드 DB 연동 (KPI 외 나머지 3개 섹션)
   · 기존 KPI(GMV/확정수익)는 이미 /api/org/dashboard 연동. 나머지 mock(플랫폼현황/처리대기/피드)을 DB 연동.
   · BE MasterDashboardController에 GET /api/org/dashboard/overview 추가:
     - platform: partner수/활성상품(on_sale)/본부(DIVISION_ADMIN)/센터(CENTER_ADMIN)/매니저(MANAGER)/버즈(BUZZ) 카운트
     - pending: 입점심사(partner_inquiry status=NEW)/출금승인(allowance_payment payment_flag=N)/민원동결(테이블 없어 0)
     - feed: 최근 sale 8건 + 최근 partner_inquiry 5건 병합·시간내림차순 top6 (상대시간 방금전/N분·시간·일 전,
       상태별 tone: 구매확정=pos/취소·반품=danger/그외=brand, 입점=warn·완료=pos)
   · repo 추가: ProductRepository.countByOnSaleTrue, PartnerInquiryRepository.countByStatus,
     AllowancePaymentRepository.countByPaymentFlag.
   · FE: components/master/MasterOverview.tsx(클라이언트, overview fetch→3섹션 렌더, 처리대기는 해당 메뉴 Link),
     master/page.tsx는 MasterKpi + MasterOverview만 렌더(mock 배열 제거).
   · 검증(admin 로그인): KPI ₩33,620,000/₩5,300, 플랫폼 3/10/2/3/4/9, 처리대기 2/0/0, 피드 6건 실데이터. 콘솔 에러 0.
   · BE 재기동: 로컬 chin4(chin4_test) 기본값으로 spring-boot:run (start-all.ps1과 동일).

  [보완 2026-07-28b] 센터/본부 최초 로그인 약관 동의 게이트
   · 요구: 센터/본부 담당자 최초 로그인 시 해당 이용약관(약관설정 CENTER/DIVISION)을 창으로 띄우고,
     [동의하기] 체크 후 동의하면 로그인ID·IP주소·동의시간·user.role 을 DB 저장.
   · 신규 테이블 term_agreement(id, login_id, term_code, role, ip, agreed_at) + JPA 자동생성.
     migration_new_fields.sql / schema_chin4.sql 반영.
   · BE terms/TermAgreement(Entity)·TermAgreementRepository(existsByLoginIdAndTermCode)·TermAgreementController
     (/api/my/agreement : GET ?code= 동의여부, POST {code} 저장). IP=X-Forwarded-For/X-Real-IP/remoteAddr,
     role=user.role, 멱등(중복 동의 시 재저장 안 함). /api/my/** = 인증 공용.
   · FE components/AgreementGate.tsx(클라이언트, 블로킹 모달): code로 동의여부 확인→미동의면 약관 노출
     (HTML/평문 자동렌더) + [동의하기] 체크 + [동의하고 시작하기]→POST→닫힘. 이미 동의면 렌더 안 함.
     center/layout.tsx(code=CENTER), division/layout.tsx(code=DIVISION)에 삽입.
   · 검증: center 로그인→게이트 노출(센터약관 내용)→동의→term_agreement 저장(center/CENTER/CENTER_ADMIN/IP/시간)
     →새로고침 재노출 안 됨. division 게이트도 노출 확인. 콘솔 에러 0.
   · ⚠ DIVISION(본부 이용약관) 내용은 아직 미등록(docs/15 원본파일 없음) → 게이트는 뜨나 본문 공란.
     본사 [약관설정]에서 본부 이용약관 내용을 등록하면 그대로 노출됨.

  [보완 2026-07-28c] 운영 도메인 chinbiz.kr 적용 시 "서버 연결 실패(백엔드 9001)" 수정
   · 원인: FE는 접속 host 기준 http(s)://chinbiz.kr:9001 로 API 호출하나, BE CORS allowed-origin-patterns 가
     localhost/127.0.0.1/175.125.94.198 만 허용 → chinbiz.kr Origin preflight 차단 → fetch throw → 연결실패 메시지.
   · 수정: application.yml app.cors.allowed-origin-patterns 기본값에 chinbiz.kr 추가
     (http/https + 포트유무 + 서브도메인: http(s)://chinbiz.kr, :* , *.chinbiz.kr, *.chinbiz.kr:*).
     CORS_ORIGINS 환경변수로 override 가능.
   · 검증(로컬 BE 재기동): Origin http/https://chinbiz.kr, :8001, admin.chinbiz.kr preflight 200 + ACAO 반환,
     evil.com 은 403. 
   · ★ 서버 반영: 서버 BE 재패키지/재기동 필요(코드 반영 완료).
   · ⚠ HTTPS 주의: 페이지를 https://chinbiz.kr 로 서비스하면 FE가 https://chinbiz.kr:9001 을 호출하는데
     BE 9001은 평문 HTTP라 실패(혼합콘텐츠/TLS). 해결: (a) http://chinbiz.kr(80)+9001 포트개방으로 서비스,
     또는 (b) 리버스프록시로 API를 https 노출(https://chinbiz.kr/api 또는 https://api.chinbiz.kr) 후
     home/.env.local·admin/.env.local 의 NEXT_PUBLIC_API_URL 에 그 주소를 지정(자동host해석 override).

  [보완 2026-07-28d] HTTPS 전환 대비 — FE API주소 해석 프로토콜 인식형으로 개선
   · 배경: 지금은 HTTP 서비스(chinbiz.kr), 추후 HTTPS 전환 예정.
   · resolveServiceUrl(home/src/lib/api.ts, admin/src/lib/auth.ts) 개선:
     - HTTP : 기존과 동일 (접속host + 포트 9001/8001/3100 직결). ← 현재 서비스 무영향.
     - HTTPS: same-origin(포트 미지정) 반환 → 리버스 프록시가 /api 를 BE(9001)로 전달하는 표준 배포 가정.
       (https 에서 :9001 직결은 평문/TLS 불일치로 불가하므로 자동 회피)
   · 검증: http://localhost:8001 접속 시 resolvedApiBase=http://localhost:9001, check-id 200 (기존 동작 유지).
   · HTTPS 전환 시 서버 준비물:
     (1) 리버스 프록시(nginx 등): https://chinbiz.kr/  → FE, https://chinbiz.kr/api/ → http://localhost:9001/api/
     (2) 위 CORS 기본값에 https://chinbiz.kr 이미 포함(보완 c) → 추가 설정 불필요.
     (3) admin/home 을 서브도메인으로 분리 서비스하면 NEXT_PUBLIC_API_URL/HOME_URL 로 override(빌드시 주입).

  [트러블슈팅 2026-07-28e] next build 타입에러 ".next/dev/types/routes.d.ts ';' expected"
   · 증상: Compiled successfully → Running TypeScript → Failed to type check,
     routes.d.ts 특정 줄이 깨진 문자열(예: 'ement/paid"...')로 잘려 있음.
   · 원인: Next(App Router/Turbopack) 자동생성 라우트 타입 캐시 손상(개발 중 새 라우트 추가 후 흔함). 소스 문제 아님.
   · 해결: 해당 앱 .next 삭제 후 재빌드. admin: rm -rf admin/.next && (cd admin && npm run build).
     PowerShell: Remove-Item -Recurse -Force .next. home 도 동일.
   · 검증: admin/.next 삭제 후 npm run build exit 0, 전 라우트(/master/settings/terms 포함) 정상 생성.

====================================================================
[추가] docs/16.txt 작업 — 알람(알림) 발송 시스템 + [알람설정] (2026-07-30)
====================================================================
■ 개요: 프로세스 발생 시 대상(버즈/매니저/센터/본부/추천인/파트너 등)에게 알람 발송. 본사 [시스템설정]-[알람설정]
   에서 프로세스별 수신대상·문구를 등록/수정. 참조: docs/chinbiz3_alramlist.xlsx(13개 프로세스).
■ 수신대상 8종(AlarmTarget): 버즈/매니저/소속센터(CENTER)/관리센터(MGMT_CENTER)/본부/본사/추천인/파트너.
■ 신규 테이블:
   - alram_setting : 프로세스×대상 1행(process_code·target UNIQUE, message, enabled, 메타). 시더가 13×8=104행 보장.
     엑셀 기본 (프로세스,대상)만 enabled=true+기본문구(총 42행), 나머지는 off+공란. 관리자가 on/off·문구 편집.
   - alram : 발생 알람(process_code,target,recipient_id/name/role,message,ref_type/id,read_flag,created_at).
   (migration_new_fields.sql / schema_chin4.sql 반영. JPA ddl-auto 자동생성)
■ BE(com.chinbiz.api.alarm): AlarmTarget/AlarmSetting/Alarm(엔티티)·리포지토리·AlarmDefs(엑셀 13프로세스 정의)·
   AlarmSettingSeeder(@Order 21)·AlarmService(fire+수신자해석+문구렌더)·AlarmSettingController
   (GET/PUT /api/org/alarm-settings, GET /api/org/alarms; MASTER 전용).
   - 문구 토큰 치환: OOO=회원명, ㅁㅁㅁㅁ=센터명, @@@=고객, $$$$$=수당, *****=상품, ______=주문번호.
   - 수신자 해석: CENTER=sales_center_id 동일 CENTER_ADMIN, DIVISION=센터 head_code→본부 idx의 DIVISION_ADMIN,
     HQ=MASTER_ADMIN, REFERRER=referral_code 회원.
■ 연동(트리거): [회원가입] AuthController.signup → alarmService.fireSignup(신규회원). (알람 실패는 가입 비차단 try/catch)
   ※ 현재 라이브 연동은 SIGNUP 1건(예시). 나머지 12개 프로세스는 설정/시드 완비 상태이며 각 트리거 지점에서
     alarmService.fire(processCode, 대상별수신자, 토큰, refType, refId) 호출만 추가하면 됨(동일 패턴).
■ FE: nav 시스템설정에 [알람설정] 추가, /master/settings/alarms + components/master/AlarmSettingsManager.tsx
   (좌 프로세스목록[수신 개수] / 우 대상8종 체크박스+문구 textarea + 저장). 약관설정과 유사 구조.
■ 검증: 시드 104행(42 enabled), 설정 GET(13프로세스)·PUT(persist+revert) 정상. 화면 렌더 정상, 콘솔 에러 0.
   ★ 테스트 실증: 추천인(buzzgn1) 있는 회원가입 → alram 5건 생성:
     CENTER(ctr_gangnam1,center)·DIVISION(div_seoul1)·HQ(admin)="알람테스트 버즈회원이 강남구센터 소속으로 가입되었습니다.",
     REFERRER(buzzgn1)="알람테스트 버즈회원이 추천인으로 가입되었습니다." (테스트 계정/알람은 검증 후 삭제).

  [보완 2026-07-30] docs/16 나머지 12개 프로세스 트리거 실연동
   · AlarmService 에 프로세스별 진입점 추가: fireMemberReferral/firePartnerInquiry/fireManagerApply/
     fireManagerApprove/fireProductRegister/fireEduApply/fireEduApprove + fireSaleEvent(SALE1/SALE2/
     MANAGER_CANCEL/ORDER_CANCEL/ORDER_CONFIRM 공통, Sale로부터 8대상 해석). 문구토큰 OOOO=회사(4자,OOO보다 먼저),
     OOO=회원, ㅁㅁㅁㅁ=센터, @@@=고객, $$$$$=수당(AllowanceService 규칙 총수당×reward/100), *****=상품, ______=주문번호.
   · 트리거 연동(각 컨트롤러 성공지점 try/catch 1줄): PartnerInquiryController.submit / BuzzMemberController.create /
     BuzzManagerController.apply / CenterManagerController.approve / ProductController.create /
     BuzzEducationController.complete / EducationAdminController.approve / BuzzSalesController.create·assign /
     AdminSalesController.reassign(매니저 초기화 前 발송) / PartnerSalesController.confirm·cancel.
   · 검증(로컬): 파트너상담→HQ, 상품등록→전체 버즈/매니저/센터 브로드캐스트, 1차영업→버즈·센터·본부·본사(CP 135,000원)
     +파트너(상품명 문구) 실증. 콘솔 에러 0.

====================================================================
[추가] docs/17.txt 작업 — 정산 알람 3종 + admin 알람 벨 (2026-07-30)
====================================================================
■ A. 알람 프로세스 3종 추가([알람설정]) — 대상 버즈/매니저/센터/본부, 문구 "0000년 00월…"(period 토큰):
   - CLOSE(마감완료) : 본사 [매출현황] [마감완료] → MasterSettlementController.closeSales
   - SETTLE(정산완료): 본사 [마감내역] [정산완료] → settle
   - PAY(지급완료)   : 본사 [정산내역] [지급완료] → pay
   AlarmService.fireSettlement(processCode, fixedMonth, members(memberType,memberId)) — allowance/allowance_payment
   의 member_type→대상(BUZZ·TOPBUZZ→버즈 / MANAGER→매니저 / BUZZ_CENTER·MANAGER_CENTER→센터 / DIVISION→본부, HQ 제외),
   memberId 중복제거·이름해석. period "202607"→"2026년 07월". AlarmDefs/시더 확장(16 프로세스×8 = 128행, CLOSE/SETTLE/PAY 각 4 enabled).
■ B. admin 우측 상단 알람 벨 (전 역할 공용):
   - Alarm 엔티티 read_at(확인완료 일시) 추가. AlarmRepository countByRecipientIdAndReadFlag / findByRecipientIdAndReadFlag.
   - MyAlarmController(/api/my/alarms): GET {unread, items(최근50)} / POST /read (미확인 전체 read_flag=Y·read_at=now).
   - components/AlarmBell.tsx(미확인 배지+팝다운, 열면 read 처리·배지 0·1분 폴링). 5개 topbar(master/partner/buzz/center/division)
     기존 정적 벨 → AlarmBell 교체(tone dark/light). recipient_id=로그인ID 기준.
   - DB: alram.read_at 추가(ddl-auto 자동, migration/schema 반영).
■ 검증: 마감완료/정산완료/지급완료 실행 → alram CLOSE/SETTLE/PAY 각 버즈·센터·본부 "2026년 07월…" 생성.
   벨 API(unread 조회·read 처리·0), 마스터 화면 벨 배지 3→클릭 팝다운 목록→배지 0, read_at 저장 실증. 콘솔 에러 0.
   ※ 테스트 데이터(미삭제): product14 정산알람테스트상품, sale54(OD20260730-000054, 202607 마감/정산/지급 완료),
     관련 allowance/allowance_payment/alram + docs/16 검증분(partner_inquiry '알람테스트파트너' 등). 정리 필요 시 삭제 가능.

====================================================================
[추가] docs/18.txt 작업 — 추천링크·모바일알림·추천마일리지·정산이월·상품설명5분리·에디터 (2026-07-31)
====================================================================
■ P1. 버즈 [버즈네트워크] 내 추천링크 → https://chinbiz.kr/signup?ref=회원id. [링크복사]는 전체 URL 복사
   (HTTPS=Clipboard API / HTTP=execCommand textarea 폴백). NetworkSection.tsx.
■ P2. 모바일 알림창(AlarmBell 팝다운) 반응형: w-[calc(100vw-1.5rem)] max-w-sm sm:w-80, max-h-[70vh] sm:max-h-96.
■ P3. [환경설정] [추천마일리지 지급] 카드(가입버즈/추천인 CP) — DB 연동.
   - 신규: app_setting(key-value) 테이블 + AppSetting/Repo/Controller(GET·PUT /api/org/app-settings) + 시더(500/500).
   - 회원가입(AuthController.signup): 추천인 없거나 미존재 → referralCode 강제 'dukebaek'. 가입버즈+추천인 모두 CP 지급.
     allowance INSERT: order_no=join_<yyyyMMddHHmmss>_<가입id>, type=JOIN, status=CP (가입버즈=BUZZ, 추천인=TOPBUZZ).
   - Allowance.Type 에 JOIN 추가. ★allowance.type 은 ENUM → 수동 ALTER 필수:
     ALTER TABLE allowance MODIFY COLUMN type ENUM('ORDER','CANCEL','CANCEL_FEE','JOIN') NOT NULL;
■ P4. 버즈 [1차영업등록] 완료 시 해당 회원(memberId=본인)의 JOIN·CP → MP 전환(confirm_date=오늘). BuzzSalesController.create.
■ P5. [정산완료](settle) 시 회원 MP 합계 1만원 미만 → 당월 미정산·익월(fixed_month+1) 이월 + [정산완료(미정산대상)]
   알람 발송("정산 대상금액이 1만원 미만…좀더 노력해 주세요~"). 1만원 이상만 정산전표 생성/paid. AlarmDefs SETTLE_SKIP 추가.
■ P6. 상품설명 5분리 + 에디터 강화:
   - Product 5컬럼(desc_guest/buzz/manager/partner/admin, longtext). 본사 ProductForm=5탭 에디터(DescTabs),
     파트너 ProductForm=파트너전용(desc_partner). 노출: 버즈마켓상세 as=buzz→desc_buzz / 매니저 as=manager→desc_manager
     (BuzzMarketController.detail?as=, ProductDetail 역할별 as 전달), 파트너 상품수정→desc_partner (미등록 시 레거시 description 폴백).
     비로그인=desc_guest(차후). ProductController DTO/apply/dto 5필드 반영.
   - RichTextEditor 강화: 글자크기(작게/보통/크게)·인용(blockquote)·정렬(좌/중/우)·구분선·글자색/배경색(color input). globals .rte-content 보강.
■ 검증(로컬): P3 signup(추천없음)→dukebaek·500/500 JOIN CP, P4 1차영업→JOIN MP전환, P5 close→settle 500<1만 미정산·익월(202608)
   이월+SETTLE_SKIP 알람, P6 상품 5설명 등록→buzz/manager/partner 각 전용설명 반환, 폼 5탭+에디터 확장 렌더. 콘솔 에러 0.
   ※ 테스트 데이터(미삭제): miltest01(가입/JOIN), product15(5분리테스트) 등. 정리 필요 시 삭제 가능.

★ DB 마이그레이션(수동 적용): app_setting/alram.read_at 신규, product desc_* 5컬럼,
   allowance.type ENUM 에 JOIN 추가 — migration_new_fields.sql / schema_chin4.sql 반영.

  [보완 2026-07-31b] 홈 포트 8001→80 (도메인 chinbiz.kr 직접 접속) + FE API 네트워크오류 처리
   · home 포트 80: home/package.json dev·start "-p 80". start-all.ps1/stop-all.ps1 HOME 포트 80.
     → http://chinbiz.kr (기본 80포트)로 홈 직접 접속. (서버 배포 시 80 바인딩 권한 필요: root/CAP_NET_BIND_SERVICE
       또는 nginx 리버스프록시 80→next. 윈도우 로컬은 80 여유 시 바로 가능.)
   · resolveServiceUrl(home api.ts / admin auth.ts): HTTP 분기에서 포트 80이면 :80 생략 → http://호스트.
     admin HOME_URL 포트 8001→80 (goToLogin=http://chinbiz.kr/login). API(9001)·admin(3100) 포트는 유지.
   · CORS(application.yml): 포트없는 오리진 추가 — http://localhost, http://127.0.0.1, http://175.125.94.198
     (기존 :* 패턴은 80 무포트 오리진 미매칭이라 홈80 접속 시 차단됨 → 무포트 오리진 명시). chinbiz.kr 는 기존 포함.
   · FE API 헬퍼 네트워크실패 처리(unhandledRejection 방지): admin/home lib/api.ts apiGet/apiPost/send/apiUpload
     를 try/catch로 감싸 실패 시 {ok:false,status:0,message:"서버에 연결할 수 없습니다."} 반환.
     AlarmBell.load()·read 도 catch 추가. (BE 미연결 시 콘솔 unhandledRejection 스팸 제거)
   · 검증: http://localhost(80) 홈 렌더 + check-id 200(CORS 무포트 오리진 통과), resolveApi=localhost:9001,
     admin /master 콘솔 에러 0. BE=200/HOME=200(80)/ADMIN=200.

  [보완 2026-07-31c] nginx 리버스 프록시 배포 예시 (chinbiz.kr)
   · deploy/nginx/chinbiz.kr.conf : nginx(80→443) + 서브도메인 라우팅
     - https://chinbiz.kr → home(내부8001), https://admin.chinbiz.kr → admin(내부3100),
       각 도메인 /api·/uploads → BE(9001). API 는 same-origin 프록시라 CORS 미발생.
     - HTTP→HTTPS 리다이렉트 + certbot(acme-challenge) 경로, 하단에 [HTTP 전용 임시] 블록 포함.
   · deploy/nginx/README.md : DNS(A 3개), 내부포트(home 80→8001 환원), FE env(빌드전 NEXT_PUBLIC_ADMIN_URL=
     https://admin.chinbiz.kr / NEXT_PUBLIC_HOME_URL=https://chinbiz.kr — 서브도메인 이동 링크용, API는 불필요),
     PM2 기동, certbot 발급, 방화벽(80/443 열고 3100/9001/8001 내부만) 절차.
   · ※ nginx 사용 시 home 은 80이 아닌 8001 내부포트로 구동(nginx가 80/443 점유).

====================================================================
[추가] docs/19.txt 작업 — 매니저 다중센터 신청(manager_center) (2026-08-01)
====================================================================
■ 신규 테이블 manager_center (buzz_id, center_id, apply_date, approve_date, status[I신청/Y승인]).
   버즈회원 매니저신청 시 최대 3개 센터 선택 → 센터별 1행. UNIQUE(buzz_id,center_id).
   migration_new_fields.sql / schema_chin4.sql 반영.
■ BE:
   - ManagerCenter 엔티티/Repo. BuzzManagerController.apply(centerIds[] 최대3, 검증·중복제거) → manager_center INSERT(I).
     status(GET) = manager_center 기준 종합(Y>I>N) + 센터별 내역(status/신청일/승인일).
   - CenterManagerController: applications/managers = manager_center(status I/Y, center=내센터), approve = 해당
     (buzz,내센터) 행 Y+승인일. user.role=MANAGER 승급 + user.manager_* denorm 동기화(공지/알람 라우팅 호환).
   - BuzzSalesController.intake = 내 승인센터(manager_center Y, 최대3)들의 customerCenterId IN → 미배정 1차영업.
     row()에 활동센터명(customer_center_id→center 표시명) 추가(intake·managed 공용).
   - ManagerCenterBackfill(@Order25): 기존 user.manager_status(I/Y)+manager_center_id → manager_center 멱등 이관.
■ FE:
   - ManagerApplyForm: 지역본부→센터 선택 후 [추가]로 최대3개 칩 관리, centerIds 전송. 신청현황=센터별 승인/심사중 배지.
   - ManagerSalesSection(버즈1차접수현황/2차영업관리): [활동센터] 컬럼 추가.
■ 검증(로컬): buzz19test 3센터(강남/강동/광명) 신청→status 3행 I, center21 승인→강남 Y·나머지 I·role MANAGER,
   intake 4건(활동센터=강남구) 반환·표시, backfill로 기존 매니저(buzzgn2/p5manager/test002 등) 이관. 콘솔 에러 0.
   ※ 테스트 데이터: buzz19test(승인 매니저)/buzz19b(신청폼용) 등.

  [보완 2026-08-01b] 다중센터 매니저 공지/PWA 미수신 문제 수정 (docs/19 후속)
   · 원인: 공지 대상해석/가시성이 user.manager_center_id(대표 1센터)만 참조 → 매니저의 2·3번째 승인센터로
     발행한 공지는 대상에서 누락(웹푸시 미발송) + 매니저 목록에도 미표시.
   · 수정(manager_center 승인(Y) 전체 활동센터 참조):
     - NoticeRecipientResolver.resolveAccounts(MANAGER): findByRoleAndManagerCenterId → managerCenterRepo
       .findByCenterIdAndStatus(targetId,'Y') 의 buzzId 매니저 전원. (웹푸시 대상 = 해당 센터 승인 매니저 전원)
     - MyNoticeController: context/mySpec/get 을 단일 managerCenterId → 승인센터 List(targetId IN centers)로 변경.
   · 검증: buzz19test(대표 21, 승인 21·22) → 강동구(22) 매니저 공지 발행 시 /api/my/notices 에 정상 노출(수정 전 미노출).
   · [관련 미수정, 참고] AlarmService.fireSaleEvent 의 MGMT_CENTER, fireManagerApply/Approve 는 아직 단일
     manager_center_id 사용 → 영업 관련 '관리센터' 알람은 대표센터에만 라우팅. 필요 시 동일 방식으로 확장 가능.

  [정리 2026-08-01c] user 매니저 denorm 필드 5종 제거 → 전부 manager_center 로 이전 (docs/19 마무리)
   · 남은 소비처 이전:
     - AllowanceService.createForAssign: MANAGER_CENTER 수당 = manager.managerCenterId → sale.customerCenterId
       (이 영업의 지역센터 = 매니저 활동센터 중 하나). 다중센터 정확.
     - AlarmService: fireManagerApply(신청 각 센터 루프·센터별 문구), fireManagerApprove(centerId 인자),
       fireEduApply(managerCenterAdmins=승인 활동센터 전원), fireSaleEvent MGMT_CENTER = sale.customerCenterId.
       + managerCenterRepo 주입, managerCenterAdmins() 헬퍼.
     - CenterSalesController: 소속매니저 = managerIdsOfCenter(manager_center Y) 헬퍼로 교체(2곳).
     - CenterManagerController.approve / BuzzManagerController.apply: user denorm 동기화 write 제거(역할 승급만 유지).
   · 제거: User.managerCenterId/managerCode/managerStatus/managerSdate/managerEdate + getter/setter,
     UserRepository.findByRoleAndManagerCenterId, ManagerCenterBackfill.java(→ SQL backfill 로 대체).
   · DB: migration_new_fields.sql 에 (1)user→manager_center INSERT IGNORE backfill (2)user 5컬럼 DROP 추가.
     schema_chin4.sql user 정의에서 5컬럼 제거. 로컬 chin4 는 컬럼 DROP 적용 완료.
   · 검증(로컬, denorm 제거 후): buzz19test status(role MANAGER·강남Y/강동Y/광명I), center21 승인매니저 목록,
     강동구(22) 공지 수신 유지, center /sales/manager 200, buzz19b 2센터 신청→MANAGER_APPLY 알람 각 센터별 발송.
     기동 오류 0.
   ★ 서버(원격 DB) 반영: migration_new_fields.sql 의 backfill INSERT → 5컬럼 DROP 순서로 1회 실행 필요.

  [보완 2026-08-01d] 영업 이벤트 알람 관리센터(MGMT_CENTER) 다중센터 확장 (요청)
   · AlarmService.fireSaleEvent: MGMT_CENTER = centerAdmins(managerActivityCenterFor(managerId, sale.customerCenterId))
     — 매니저의 승인 활동센터(manager_center Y) 중 이 영업의 지역센터로만 매칭(다른 센터로 미확산).
     헬퍼 managerActivityCenterFor(managerId, saleCenterId): saleCenterId 가 매니저 승인센터에 포함되면 그 값, 아니면 null.
   · ★소속센터(BUZZ_CENTER=CENTER 타겟)는 그대로 buzz.sales_center_id(대표센터) 매칭 유지(요청).
   · 검증: sale56(매니저 buzz19test 활동 21·22, 영업지역 21) 구매확정 → MGMT_CENTER=center21 관리자(center,ctr_gangnam1)만,
     center22(ctruitest01) 0건. BUZZ_CENTER=버즈(miltest01) sales_center_id=30(대표). MANAGER/DIVISION/REFERRER/BUZZ 정상.


====================================================================
[추가] docs/20.txt 작업 — 매니저신청 현황/관리 + RBAC 담당자 지정 외 (2026-08-07)
====================================================================
■ Task1. 버즈admin [버즈네트워크]: 매니저 신청건이 있으면 [매니저신청현황] 버튼 노출.
   · NetworkSection.tsx — mgrStatus!=="N" 이면 [매니저신청현황] 링크(→ /buzz/network/manager-apply,
     docs/19에서 센터별 신청 현황 렌더링). 신청 전(N)에는 기존 [매니저 신청하기].

■ Task2. 버즈admin [내정보수정]: 소속센터명·추천회원(마스킹)·매니저 활동신청 지역 노출.
   · Mask.java — userId(v): 앞 4자 유지 + 나머지 X. (홍길동→홍O동 name 마스킹은 기존 사용)
   · UserSelfController.dto(): salesCenterName(center_code), referrerLabel(Mask.name+"("+Mask.userId+")"),
     managerCenters[{centerName,status}] (manager_center findByBuzzId) 추가. CenterCodeRepository/
     ManagerCenterRepository 주입 + centerName() 헬퍼.
   · ProfileForm.tsx — Me 타입 확장 + 계정 섹션에 소속센터/추천회원 + "매니저 활동신청 지역"(센터별 승인/심사중 배지).

■ Task3. 본사 [환경설정] 추천마일리지: 미입력/미존재 시 CP 지급대상(추천인) 설정화(하드코딩 dukebaek 제거).
   · AppSettingController — JOIN_FALLBACK_REF="join_fallback_ref"(기본 dukebaek). list() putIfAbsent+Seeder.
   · AppSettingRepository.getStr(code,def). AuthController.signup — fallbackRef = getStr(JOIN_FALLBACK_REF).
   · settings/page.tsx — fallbackRef 입력칸 "미입력/미존재 시 [___] 에게 CP 지급됨" 저장 연동.

■ Task5. 본사 [알람설정]: [매니저승인] 뒤에 [매니저취소](MANAGER_APPROVE_CANCEL) 알람 추가.
   · AlarmDefs — MANAGER_APPROVE_CANCEL: [매니저]"매니저 활동량이 미비하여, 매니저 승인이 취소되었습니다."
     [센터]"OOO회원(@@@)의 매니저 활동량이 미비하여, 매니저 승인이 취소되었습니다." (MANAGER_APPROVE도 센터 문구 보강)
   · AlarmService — fireManagerApprove(manager,centerId) 센터 타겟 추가, fireManagerApproveCancel(manager,centerId) 신설.

■ Task6. 본사 [조직망 및 영업관리] [회원리스트] 뒤 [매니저신청] 메뉴 신설(탭 신청접수/승인완료).
   · MasterManagerController(/api/org/manager-applications): GET ?status=I|Y(전 센터), POST .../{buzzId}/{centerId}/approve
     (mc.status=Y·role=MANAGER·fireManagerApprove), .../cancel(fireManagerApproveCancel→mc 행 삭제, 남은 Y 없으면 role=BUZZ).
     승인완료(Y) 행은 최근3개월 2차영업 실적(건수·상품판매가 합) 포함.
   · nav.ts 메뉴 추가, /master/organization/manager-applications 페이지 + ManagerApplications.tsx(탭 테이블).

■ Task4. ★RBAC 담당자 지정 + 메뉴 접근 제어★
   · 저장: admin_scope 테이블(login_id PK, areas CSV). 행 없음 = 슈퍼(전체 접근).
     영역 A=파트너·상품·교육 / B=조직망·영업 / C=수당·정산 / D=공지·민원 / (시스템설정=SUPER 전용).
   · BE: AdminScope 엔티티/repo, AdminScopeController(/api/rbac/admin-scopes) GET/PUT/DELETE.
     - requireSuper(): 슈퍼(미지정 MASTER_ADMIN)만 RBAC 관리 가능(아니면 403).
     - 자기잠금 방지: 슈퍼가 본인 계정에 영역 지정 시도 → 400 차단.
     - /api/auth/me 응답에 adminScopes(A~D) 추가(MASTER_ADMIN만).
   · FE: nav.ts 각 메뉴에 area 부여 + visibleNav(scopes)/pathAllowed(path,scopes)/isSuperScope 헬퍼.
     MeContext(AuthGuard가 me 공급) → Sidebar가 visibleNav 로 메뉴 필터. AuthGuard가 담당영역 밖 경로 직접진입 시
     허용 첫 메뉴로 리다이렉트. settings RBAC 카드 → RbacManager(계정×영역 체크박스 편집, 전체해제=슈퍼).
   · 검증(로컬): admin=슈퍼(전체 8메뉴+시스템설정). 테스트계정에 [A,C] 지정 → 사이드바 대시보드+파트너+상품+교육+
     수당정산만, /settings·/organization 직접진입 → /master 리다이렉트. me.adminScopes=[A,C]. 제한계정 RBAC관리 403,
     본인 영역지정 400. 재기동 후 지정 영속. (테스트계정/전표/알람 정리 완료)

■ DB: admin_scope 신규 테이블 → migration_new_fields.sql / schema_chin4.sql / 위 [DB 마이그레이션] 섹션 반영.
   AlarmDefs·app_setting 은 코드 시드(기동 시 자동). 신규 테이블 외 스키마 변경 없음.
   ★ 서버(원격 DB): admin_scope CREATE TABLE 1회 적용(또는 BE ddl-auto=update 로 자동 생성).

  [보완 2026-08-07b] 버즈 [매니저신청현황] — 신청지역별 상태 표시 + 3개 미만 시 지역 추가 신청 (요청)
   · 화면(ManagerApplyForm.tsx): "매니저 신청 현황" 카드에 신청 지역별 상태(승인/심사중·신청/승인일자) 나열 +
     신청 개수 < 3 이면 "활동 지역 추가 신청" 폼 노출("현재 N개 신청됨 · M개 더 신청 가능"). 3개 완료 시 폼 숨김.
     센터 드롭다운은 이미 신청한 센터 제외, 이번 선택 개수는 남은 슬롯(3-기신청)까지. 제출 후 status 재조회.
   · BE(BuzzManagerController.apply): 기존 I/Y 있으면 무조건 차단 → "총 3개 미만이면 추가 신청 허용"으로 변경.
     기존 I·Y 센터 제외·중복 거부, 남은 개수(3-기존) 초과 거부, 이미 3개면 "이미 최대 3개" 거부. role BUZZ·MANAGER 허용.
   · BE(AlarmService): fireManagerApply(applicant, centerIds) 오버로드 추가 → 추가 신청 시 이번 센터에만 [매니저신청]
     알람 발송(기존 신청센터 중복 알람 방지). 무인자 버전은 I 상태 전체로 위임(기존 호출부 호환).
   · 검증(로컬 E2E): 신규버즈 apply[21]→1개, 중복[21]→400, +[22,259]→3개, [46]→400(최대3). 브라우저:
     현황 1개→추가폼(2개 가능)→강서구 추가·약관동의·제출→2개(1개 가능)→3번째 후 폼 숨김+"모두 신청되었습니다". (테스트계정 정리)

  [보완 2026-08-07c] [담당자지정]에 [담당자 등록] 버튼 + MASTER_ADMIN 회원가입 (요청)
   · BE(AdminScopeController): POST /api/rbac/admin-scopes/register (슈퍼 전용) — {userId,password,name,email,phone,areas?}
     로 role=MASTER_ADMIN 계정 생성(BCrypt). AccountValidation(아이디 영숫자4~20·비번10자+) + user/partner 아이디 중복검사.
     areas 지정 시 admin_scope 동시 저장. PasswordEncoder·PartnerRepository 주입, parseAreas/str 헬퍼로 정리.
   · FE(RbacManager.tsx): 섹션 우측 상단 [+ 담당자 등록] 버튼 → 모달(아이디/비번/이름/휴대폰/이메일 + 담당영역 A~D 토글).
     클라 검증(정규식·10자·이름) 후 register 호출, 성공 시 모달 닫고 목록 재조회. 등록된 담당자는 리스트에 표시되고
     기존 체크박스로 영역 변경/전체해제 가능.
   · 검증: BE E2E(등록201·중복409·짧은비번400·잘못된아이디400·비슈퍼403·신규계정 /me adminScopes) + 브라우저(모달 등록→
     리스트 즉시 반영, D영역 지정 확인). 테스트계정(opadmin1·csadmin1) 정리 완료.

  [보완 2026-08-07d] [담당자 등록] 화면에 [아이디 중복체크] 버튼 (요청)
   · BE(AdminScopeController): GET /api/rbac/admin-scopes/check-id?userId= (슈퍼 전용) → {available} (user+partner 양쪽 검사).
   · FE(RbacManager.tsx): 아이디 입력칸 옆 [아이디 중복체크] 버튼 + 결과 문구(사용 가능/이미 사용 중). 아이디 변경 시
     결과 무효화, 등록 제출은 중복확인 통과(available) 시에만 허용("아이디 중복확인을 해주세요" 가드).
   · 검증(BE): admin/partner→available:false, freshid→true. 브라우저: 모달에서 admin→"이미 사용 중",
     변경 후 newadmin7→결과 초기화→체크→"사용 가능" 확인.

  [보완 2026-08-07e] [담당자지정] 리스트 상태(운영/중지) 표시 + 토글 버튼 (요청)
   · BE(AdminScopeController): list()에 status 추가(user.status, null/기타→ACTIVE, STOP만 STOP).
     PUT /api/rbac/admin-scopes/{userId}/status {status:ACTIVE|STOP} (슈퍼 전용, MASTER_ADMIN 대상, 본인 중지 불가).
   · BE(AuthController): status=STOP 계정은 로그인 403("중지된 계정입니다") + /me 403 → 중지 시 접근 차단.
     (user.status 는 기존 필드 재사용, NULL=ACTIVE 취급. STOP 문자열만 차단.)
   · FE(RbacManager.tsx): 리스트 오른쪽 끝에 [상태] 컬럼 — 운영중(초록)/중지(빨강) 배지 + 토글 버튼
     (ACTIVE→[중지], STOP→[운영]). [중지] 클릭 시 confirm 후 status=STOP, [운영] 클릭 시 status=ACTIVE. 즉시 반영.
   · 검증: BE E2E(등록ACTIVE→로그인ok→STOP→로그인403→본인중지400→ACTIVE복구→로그인ok) + 브라우저([상태]컬럼 렌더,
     [중지] 클릭→배지 '중지'/버튼 '운영'으로 토글). 테스트계정(stopadm1) 정리 완료.

  [수정 2026-08-07f] 본사 [회원정보수정](OrgMemberForm) 역할 표시 오류 + 하위추천회원 전체변경 버튼 노출 제한 (요청)
   · 버그: /api/org/members 는 전 역할(BUZZ/MANAGER/CENTER/DIVISION/MASTER)을 반환하나, 수정화면 역할표시가
     "DIVISION_ADMIN ? 본부 : 센터" 로만 처리되어 버즈/매니저 등도 '센터(CENTER_ADMIN)'로 잘못 표시됨.
   · 수정(OrgMemberForm.tsx): ROLE_LABEL 맵으로 initial.role 실제 라벨 표시(버즈회원/관리매니저/센터/본부/본사관리자/파트너사).
     [하위추천회원 소속센터 전체변경] 버튼·안내문구는 CASCADE_ROLES=[BUZZ,MANAGER,CENTER_ADMIN] 인 회원(수정모드)만 노출.
   · 검증(브라우저): id9 BUZZ→"버즈회원 (BUZZ)"+버튼O, 16 MANAGER→"관리매니저"+버튼O, 13 CENTER→"센터"+버튼O,
     12 DIVISION→"본부"+버튼X, 10 MASTER→"본사관리자"+버튼X. FE 전용(BE 무변경).

  [수정 2026-08-07g] chinbiz.kr/master 404 — home→admin 리다이렉트 도메인 오류 (요청)
   · 배포구조(deploy/caddy·nginx): chinbiz.kr→home(8001), admin.chinbiz.kr→admin(3100). /master 는 admin 앱 라우트.
     → chinbiz.kr/master 는 home 앱으로 가서 404 (정상). admin 은 https://admin.chinbiz.kr/master 에 있음(실서버 응답 확인).
   · 버그: home 로그인 후 리다이렉트 ADMIN_URL 이 HTTPS 에서 same-origin(chinbiz.kr)으로 계산 → chinbiz.kr/master 로 보내 404.
   · 수정:
     - home/src/lib/api.ts resolveAdminUrl(): HTTPS 면 admin.<도메인> 서브도메인, HTTP 면 host:3100. login/page.tsx 적용.
     - admin/src/lib/auth.ts resolveHomeUrl(): HTTPS 면 admin. 접두어 제거한 루트도메인(home), HTTP 면 host(:80).
     - 토큰 쿠키는 이미 Domain=.chinbiz.kr 로 저장돼 서브도메인 공유 OK(변경 없음).
   · ★ NEXT_PUBLIC 계열 런타임 해석이지만 클라이언트 코드 → home·admin 재빌드/재배포 필요.
     (서버: home `npm run build && 재기동`, admin `npm run build && 재기동`. 로컬 http 동작은 기존과 동일 host:3100/:80.)
   · 대안(단일도메인 원할 시): admin 에 basePath 도입 후 chinbiz.kr/<prefix>/* 프록시 — 미채택(서브도메인 구조 유지).

  [수정 2026-08-07h] 모바일/PWA 진입점(start_url "/") 세션 기반 자동 라우팅 (요청)
   · 기존: admin/src/app/page.tsx = "친비즈 어드민 런처" 카드 목록(docs/mobile.jpg) 노출.
   · 변경: 런처 미노출. 마운트 시 세션 판별 → 토큰 유효(fetchMe): ROLE_PATH[role] 대시보드로 이동 /
     토큰 없음·무효: 로그인(/login)으로 이동. (이동 전 짧은 "이동 중…" 로딩만 표시)
   · PWA manifest start_url="/" 이므로 모바일앱 실행 시 세션 있으면 바로 역할 대시보드, 없으면 로그인 화면.
   · 검증(localhost:3100): 무세션 "/"→/login, MASTER_ADMIN 세션 "/"→/master. 런처 화면 미노출 확인.

  [수정 2026-08-07i] 실서버 상품 이미지 미표시 (home 메인·buzz 마켓·master 상품) — 업로드 URL 절대경로 문제 (요청)
   · 원인: UploadController 가 ServletUriComponentsBuilder.fromCurrentContextPath() 로 절대 URL 저장 →
     리버스 프록시 뒤에서 http://내부호스트(또는 http://chinbiz.kr) 로 굳어져, HTTPS 페이지에서 mixed-content 차단·호스트 불일치로 이미지 안 보임.
   · 수정:
     - BE UploadController: 반환 URL 을 상대경로 "/uploads/<name>" 로 변경(호스트/프로토콜 비의존).
     - FE mediaUrl(p) 헬퍼(home/src/lib/api.ts, admin/src/lib/api.ts): 상대(/uploads/..)·레거시 절대(http://host:9001/uploads/..)
       모두 현재 API_BASE 기준 재구성(https=same-origin, localhost=:9001) → mixed-content/호스트불일치 해소. DB 마이그레이션 없이 기존 데이터도 표시됨.
       적용: MarketSection, ProductDetail(buzz), master/partner ProductForm 미리보기, home ProductShowcase·ProductBrowse.
     - home·admin next.config.ts: 개발용 rewrite /uploads/:path* → BE(9001). (운영은 프록시가 /uploads 를 BE 로 먼저 라우팅 → 무해)
       → 상품설명 HTML 내 <img src="/uploads/..">(RichTextEditor 신규 업로드=상대경로) 도 로컬/운영 모두 표시.
   · 검증(localhost): 이미지 파일 BE 직결/8001·3100 rewrite 모두 200. home 랜딩 상품 img naturalWidth>0 로드 확인.
   · ★ 서버 반영: BE 재기동(업로드 상대경로) + home·admin 재빌드·재배포(mediaUrl·rewrite). 기존 상품 썸네일은 mediaUrl 로 즉시 정상.
   · (선택) 레거시 상품설명 HTML 내 절대 이미지경로 정규화 SQL(서버 chin4, 1회):
       UPDATE product SET desc1=REPLACE(REPLACE(desc1,'https://chinbiz.kr/uploads/','/uploads/'),'http://chinbiz.kr/uploads/','/uploads/') WHERE desc1 LIKE '%/uploads/%';
       (desc2~desc5, image1~image5 도 동일 패턴으로 필요 시 정규화. 썸네일은 mediaUrl 로 이미 동작하므로 필수 아님.)

====================================================================
[추가] docs/21.docx 작업 — 홈 카운트업 게이트·admin로그인·버즈대시보드 개편 등 8건 (2026-08-12)
====================================================================
■ 1. 홈 [카운트업] 밴드: 누적 영업 매칭 ≥100건일 때만 노출(<100 감춤).
   home Stats.tsx: total 상태(로드전 null=미노출) + MIN_TOTAL_MATCHES=100, section 래퍼를 Stats 내부로 이동
   (total===null||<100 → return null). page.tsx 는 <Stats/> 만 렌더. (로컬 total=28 → 미노출)
■ 2. admin 로그인 화면에 [← 친비즈 홈으로 가기] 링크 추가 → HOME_URL(운영 https://chinbiz.kr).
■ 3. 버즈 메뉴 순서 변경(BuzzTopbar NAV): 대시보드|영업파이프라인|상품마켓|수당현황|정산현황|버즈네트워크|공지사항
   (기존 수당/정산 드롭다운 → 수당현황·정산현황 개별 항목). 매니저 NAV 는 유지.
■ 4. 버즈 대시보드 전면 개편(DashboardSection.tsx, /buzz): ①환영+주요용어 버튼+수당요약(출금가능MP/예정CP, 클릭이동)
   ②영업 파이프라인 5단계 카운트(리드접수/매니저실사/계약대기/최종성공/정산완료)+실시간 최근메시지+[상세보기>]
   ③상품마켓 2×3 그리드+[전체보기>] ④버즈네트워크(추천코드·링크·복사토스트·추천수·네트워크수익)+[자세히>]
   ⑤공지 최근2건+[더보기>]. page.tsx: 버즈=DashboardSection / 매니저=WalletSection 분기.
   BE: BuzzDashboardController.GET /api/buzz/dashboard/summary(파이프라인 5단계 카운트[본인+추천버즈 범위]·최근메시지·
   추천버즈수·네트워크누적MP·추천코드). status 매핑: 접수→리드접수/상담·방문→매니저실사/계약체결→계약대기/배송·설치→최종성공/구매확정→정산완료.
■ 5. [친비즈 주요 용어 보기] → 레이어 팝업(용어 7종: 영업파이프라인/상품마켓/CP/MP/마감·정산/OTA/MRA). DashboardSection 내 모달.
■ 6. [영업파이프라인] 타이틀 아래 안내문구 추가 — PageHead 에 note prop 신설(💡 강조 박스, chipBox 테마).
   "영업 파이프라인은 가망고객이 계약까지 가면서 거치는 거래의 상태와 진행 단계를 보여 줍니다."
■ 7. [상품마켓] 검색블록과 리스트 사이 간격+경계선(MarketSection: mb-6 border-b pb-5).
■ 8. [버즈네트워크] 회원명 클릭 → [회원정보 보기](읽기전용): 타이틀 '수정→보기', 기본정보·연락처 readOnly,
   주소 시·도/구·군까지만(addressGuGun), 정산계좌 섹션 제거, 저장버튼 제거(목록으로만). MemberForm 은 new(등록)만 편집.
■ 검증: BE 컴파일·양 FE 타입체크 통과. summary 엔드포인트 응답 확인(신규 버즈: 5단계 0·추천 0). 전 라우트 200
   (home/ · /buzz · /login · /buzz/pipeline · /buzz/network/9). 브라우저 시각검증은 확장 미연결로 생략(로직·라우트 검증 대체).
■ BE 무DB변경(신규 테이블 없음). 서버 반영: BE 재기동 + home·admin 재빌드·재배포.

====================================================================
[추가] docs/22.txt 작업 — 마켓필터·수당카드·1차영업고정·주민번호·정산알람 등 9건 (2026-08-12)
====================================================================
■ 1. 버즈[상품마켓]·매니저[관리마켓] 검색에서 [카테고리]·[파트너사] 필터 제거(상품명만). MarketSection.tsx(cats/ptns state·fetch 제거).
■ 2. home 푸터: [통신판매업신고번호…] 삭제, 이메일 help@chinbiz.com → mocanet@naver.com. Footer.tsx.
■ 3. 버즈 대시보드 수당카드 좌우 교체: CP(예정,좌)·MP(출금가능,우). DashboardSection.tsx.
■ 4. 버즈 [1차영업 등록](SaleForm): 마켓 진입(productId) 시 카테고리·상품 select 고정(disabled),
   [고객 기본정보]→[가망고객 기본정보], 영업진행상태 select 고정(신규=접수).
■ 5. 버즈 [버즈네트워크] "내 추천 링크 (친쿠)" → "내 추천 링크"(친쿠 제거). NetworkSection.tsx.
■ 6. 모바일 버즈admin 상단 [매니저admin 보기] 버튼 노출: 모바일=[M]/[B] 아이콘, 데스크톱=전체문구. BuzzTopbar.tsx.
■ 7. 본부/센터/버즈 내정보에 [주민등록번호 (세금신고용)] 필드 추가 + 유효성검사 + user.resident_number 저장.
   BE: User.residentNumber(컬럼 resident_number), common/ResidentNumber(13자리+검증부호), UserSelfController(dto+SelfUpdate+검증저장, 빈값=해제).
   FE: buzz/division/center ProfileForm 3종에 필드+클라 13자리 검증. (평문 저장·요구사항, 잘못된 번호 400)
■ 8. 본사 [정산내역](MasterSettlement payments)에 주민번호 등록여부 컬럼 + 미등록 회원 [알람발송] 버튼.
   BE: MasterSettlementController.paymentRows(residentRegistered/residentApplicable), POST /api/org/settlement/notify-resident,
   AlarmDefs "RESIDENT_NUMBER_REQUEST"(BUZZ/MANAGER/CENTER/DIVISION), AlarmService.fireResidentNumberRequest(role→target 1인 발송).
   FE: residentCol=true, 등록/미등록 배지 + [알람발송](발송 후 '발송됨' 비활성).
■ 9. HOME 회원가입 sales_center_id: 추천인 있으면 추천인의 sales_center_id, 없으면 기본회원(join_fallback_ref)의 sales_center_id.
   AuthController.signup (주소매칭·입력값 우선순위 제거, 추천인/기본회원 승계로 단순화).
■ DB: user.resident_number VARCHAR(20) 추가 → migration_new_fields.sql / [DB 마이그레이션] 반영. AlarmDefs 코드시드(기동 자동).
   서버 적용: ALTER TABLE `user` ADD COLUMN `resident_number` varchar(20) DEFAULT NULL; (또는 ddl-auto=update 자동)
■ 검증(로컬): BE 컴파일·양 FE 타입체크 통과. E2E — 추천인 가입 scid 승계(101), RRN 무효 400·유효 저장,
   정산 notify-resident 미등록회원 알람발송 200(alram 생성). 전 라우트 200. 서버 반영: BE 재기동 + home·admin 재빌드.

  [수정 2026-08-12b] npm run build 실패(next/font/google Noto Sans KR 빌드시 다운로드 불가) 해결
   · 원인: layout.tsx 가 next/font/google 로 Noto Sans KR 를 빌드타임에 다운로드→서버가 Google Fonts 미접속 시 build 실패(module-not-found).
   · 조치(home·admin 동일): next/font/google 제거 → <link>(preconnect+stylesheet, fonts.googleapis.com Noto Sans KR)로 런타임 로드.
     globals.css --font-sans: var(--font-sans-kr) → "Noto Sans KR"(+기존 시스템 폰트 fallback). Google 미접속 시 시스템 폰트로 graceful fallback.
   · 검증: home·admin `npm run build` 모두 성공(exit 0). 빌드타임 외부 폰트 다운로드 의존 제거.

====================================================================
[추가] docs/23.docx 작업 — 파이프라인 컬럼순서·매니저 메뉴·매니저 대시보드·활동가이드 4건 (2026-08-14)
====================================================================
■ 1. 버즈 [영업파이프라인] 컬럼 순서 변경: 등록일|상품명|고객명|수당금액|영업단계|구분|주문번호 (파트너사 컬럼 제거). PipelineSection.tsx.
■ 2. 매니저 메뉴 순서: 대시보드|교육관리|관리마켓|영업관리(버즈1차접수현황/2차영업관리)|수당현황|정산현황|공지사항. BuzzTopbar.tsx(NAV_MANAGER, SETTLEMENT_NAV 제거).
■ 3. 매니저 대시보드 신설(ManagerDashboardSection.tsx, /buzz 매니저뷰): ①환영+관할구역(manager_center Y 센터명)+CP(예정)/MP(출금가능,→수당현황)
   ②관리매니저 활동가이드(Step1교육이수→Step2자동배정→Step3 1차배정→Step4 2차현장이행)+[접기/열기]→가이드팝업
   ③관할지역 신규 1차접수 리드(/sales/intake, [⚡즉시 배정받기]=assign→2차영업관리, 교육미승인시 [교육이수 후]) 
   ④진행중 2차영업(상담대기/방문·실사/계약보조/최종성공/정산완료 5단계, /sales/managed status 집계)+[실사리포트·사진작성]
   ⑤이수필요/추천 관리상품(교육완료=products?as=manager 자동배정토글 / 미이수=/education [10분 교육수강]) ⑥공지 최근2건.
   page.tsx: 매니저=ManagerDashboardSection / 버즈=DashboardSection 분기(기존 WalletSection 매니저뷰 대체).
■ 4. [관리매니저 핵심 활동 가이드] [접기/열기] → 4-Step 레이어 팝업(createPortal 뷰포트 중앙). ManagerDashboardSection 내.
■ BE 무변경(기존 엔드포인트 재사용: dashboard?as=manager, sales/intake·managed, products?as=manager, education, my/notices).
■ 검증: admin 타입체크 + `npm run build` 성공(exit 0). /buzz·/buzz/pipeline 200. 서버 반영: admin 재빌드·재배포.

  [보완 2026-08-14 docs/23_1] 매니저 대시보드 가이드/정렬/카메라 3건
   · 1. 활동가이드 카드: 제목 옆 ⓘ(?) 원형 버튼=핵심 가이드 4-Step 팝업 열기, [접기/열기] 버튼=카드 본문 토글
     (guideCollapsed 상태, 접기 시 Step 목록 숨김). 기존엔 접기/열기가 팝업을 열던 것을 분리.
   · 2. 5단계 스텝 가운데 정렬(justify-center): 매니저 [진행중 2차영업]·버즈 [영업 파이프라인 현황] 둘 다.
   · 3. [실사 리포트 및 사진 작성하기]: <label>+<input type=file accept=image/* capture=environment>로
     모바일 클릭 시 폰 카메라 실행(촬영 후 /buzz/managed 이동), PC(≥sm)는 sm:hidden 으로 미노출.
   · 검증: admin 타입체크 통과, /buzz 200. FE 전용.

====================================================================
[추가] docs/24.docx 작업 — 하이픈자동/푸터/파트너상품보기/배정상품선택/홈링크/센터·본부 개편 (2026-08-20)
====================================================================
■ 1. 버즈 [1차영업 등록](SaleForm): 사업자등록번호·회사전화·휴대폰 입력 시 길이별 하이픈 자동 삽입(fmtBiz/fmtPhone).
■ 2. HOME 푸터 주소(서울특별시 강남구 테헤란로) 삭제. Footer.tsx.
■ 3. 파트너 [상품관리]: [+상품등록] 버튼·리스트 [수정] 컬럼 삭제(ProductsSection). 상품정보(edit)에서 [변경저장] 제거·[돌아가기](router.back()) 추가(partner/ProductForm).
■ 4. 매니저 [버즈1차접수현황] [영업권 확보] 모달(AssignModal)에 카테고리·상품 선택 추가 → 저장 시 sale 반영.
   BE BuzzSalesController.assign: AssignRequest 에 categoryId/productId 추가, 교육승인 판정 전 sale 에 set.
■ 5. 전체 admin 상단 알림 옆 [홈] 아이콘(AdminHomeLink)→HOME 이동(master/buzz/center/division/partner 5개 Topbar).
   HOME 헤더: 로그인 상태면 [로그인]→[ADMIN 바로가기](role별 admin URL), 비로그인은 [로그인] 유지(Header.tsx, /me 조회).
■ 6. 센터 admin: 메뉴 [대시보드|센터관리마켓|관할리드관제(1·2차영업관리)|매니저/버즈관리(소속버즈·매니저신청·매니저관리)|센터수당/정산|공지사항].
   센터관리마켓 스텁 페이지 신설. 대시보드=CenterDashboard(헤더 관할/매니저·버즈수 + 센터 오버라이딩 수수료 실데이터 + 매니저 가동현황 표).
   ※ 배정지연 관제·상품별 기여도 TOP3 패널은 신규 집계 엔드포인트 필요 → '준비 중' 표기.
■ 7. 본부 admin: 메뉴 [본부분석|광역상품마켓(차후·스텁)|센터별실적비교|광역프로모션(차후·스텁)|본부수당/정산|공지사항].
   본부분석=DivisionDashboard(헤더 + 광역 오버라이딩 수수료 실데이터 + 산하 센터별 실적비교 재사용). 광역프로모션·리드이관 패널은 '준비 중'.
■ 검증: BE 컴파일 + admin `npm run build` 성공(exit 0, /center/market·/division/market·/division/promotion 생성) + home 타입체크. 
   서버 반영: BE 재기동 + home·admin 재빌드. ※ 6·7의 데이터 집계 패널(수수료 상세·배정지연·기여도·리드이관)은 후속 BE 작업 필요.

  [추가 2026-08-20 docs/24_1] 전화/주민번호/사업자번호 입력 자동 하이픈 공통 적용
   · 공용 포맷 유틸 신설: admin/src/lib/format.ts, home/src/lib/format.ts (formatPhone·formatBiz·formatRRN, 숫자만+자리수별 하이픈).
   · 1. 내정보수정(전화·주민번호): buzz/center/division ProfileForm(phone+RRN), partner ProfileForm(phone). (매니저=버즈 폼 공용)
   · 2. HOME: signup 휴대폰(formatPhone), PartnerApply 사업자번호(formatBiz)·담당자연락처(formatPhone).
   · 3. 본사admin: PartnerForm 사업자번호·회사전화·담당자연락처, OrgMemberForm(본부·센터등록) 전화, RbacManager 담당자등록 휴대폰.
   · (기존 docs/24 SaleForm 은 자체 fmtBiz/fmtPhone 유지.) 검증: admin `npm run build` 성공(exit0)·home 타입체크 통과.

  [수정 2026-08-21] home 상품둘러보기(/products) 페이지 간 카드 폭 불일치 → scrollbar-gutter 고정
   · 원인: /products(ProductBrowse)는 max-w-6xl + grid-cols-3 로 페이지별 동일 구조이나, 페이지 이동 시 목록 길이가 달라
     세로 스크롤바 유무가 바뀌며 콘텐츠 폭이 ~스크롤바 폭만큼 변동 → 카드 폭이 페이지마다 다르게 보임.
   · 조치: home globals.css html 에 scrollbar-gutter: stable 추가 → 스크롤바 폭 항상 예약, 모든 페이지 콘텐츠 폭 동일.
   · 참고: 두 스크린샷이 서로 다른 브라우저 창 폭에서 캡처된 경우라면 max-w-6xl 반응형 스케일링(정상)이며, 동일 창에서는 일치.

  [보완 2026-08-21b] /products 카드 폭 불일치 추가 조치
   · ProductBrowse 카드/내부에 min-w-0 + 텍스트 break-words 추가(그리드 아이템이 콘텐츠로 트랙을 넘겨 폭이 벌어지는 것 방지).
   · home .next 삭제 후 clean rebuild(Turbopack HMR 잔여 CSS 제거). scrollbar-gutter: stable 유지.
   · 참고: 두 캡처 폭비(≈1390 vs ≈960)가 브라우저 줌 120% vs 83%와 일치 → 페이지 이동 중 Ctrl+휠 줌 변경 가능성.
     사용자에 Ctrl+0(줌 100%)·Ctrl+Shift+R(하드 리프레시) 후 재확인 안내.

  [수정 2026-08-21c] /products 페이지별 카드폭 불일치 — 진짜 원인/해결 (콘솔 측정: 1페이지 1152 / 2페이지 804)
   · 원인: home body 가 flex flex-col(sticky footer). 그 직계 자식 <section mx-auto max-w-6xl> 는 margin:auto 로 인해
     stretch 대신 내용맞춤 축소(shrink-to-fit) → 상품 수 적은 페이지(2p, 4개)에서 섹션이 804px 로 줄어 카드가 좁아짐.
   · 해결: 해당 섹션에 w-full 추가(항상 전체폭, 최대 max-w-6xl 유지). products + 동일패턴 페이지(about·partner-apply·process·terms)도 적용.
     (app/page.tsx 는 <main> 래핑이라 무관.) 검증: home clean build 성공(exit0). 앞서 scrollbar-gutter·min-w-0·break-words 도 유지.

====================================================================
[추가] docs/25.txt 작업 — 상품 확장필드 + HOME 상품상세 + 파트너 대시보드 (2026-08-21)
====================================================================
■ 1. 본사 상품등록 확장 (Product 필드 신규):
   - 월관리상품/AS지원 여부(monthly_care/as_support) → 폼 '상품 특성' 토글(키워드 태그 위)
   - 상품영상URL(video_url) → 상품설명 위 입력칸
   - 핵심스펙/효과·영업대상·상품특성·처리프로세스(spec_effect/sales_target/product_feature/process_flow, LONGTEXT)
     → 상품설명 아래 RichTextEditor 4종. ProductController Request/dto/apply + master ProductForm 반영.
■ 2. HOME 상품 [상세정보보기] → /products/[id] 상세페이지 신설(goods_view.docx):
   BE GET /api/public/products/{id}: 로그인 토큰 있으면 역할별 설명(descBuzz/Manager/Partner/Admin), 없으면 descGuest(→description 폴백).
     이미지 배열·videoUrl·월관리/AS·확장 4종·버즈/매니저 수당(원 환산 buzzRewardWon/managerRewardWon)·파트너 다른상품(최근5) 반환.
   FE: 메인+소이미지(hover 전환), 동영상 자동재생(YouTube/Vimeo 임베드), 역할별 상세설명, 확장 4종(내용 있을때만),
     파트너 다른상품 1라인5개, 고정영역 수당, 고정 하단바 역할별 CTA(비회원=가입/매니저신청, 버즈=가망고객접수/URL복사).
   ProductBrowse·ProductShowcase [상세정보보기] 링크 → /products/{id}.
■ 3. 파트너 대시보드(partner.docx image1): BillingSection 을 실시간 Billing 연동 배지 + CP(진행중,라이트)·MP(정산대상,다크네이비)
     카드 + 예치금계좌/가상계좌 + 충전·자동차감·명세서·세금계산서로 정비. PageHead '친비즈 본사 지급 수당 및 예치금 현황'.
     (중간 검정 네모 placeholder 미구현 지시 준수)
■ DB: product 7컬럼 추가 → migration_new_fields.sql 반영(ddl-auto 자동). 검증: BE 컴파일·양 FE 타입체크·전 라우트 200,
     detail 엔드포인트 응답(buzzRewardWon 환산) 확인. 서버 반영: BE 재기동 + home·admin 재빌드.

[추가] docs/25 후속 — 상품상세 4건 수정 (2026-08-28)
====================================================================
■ 1. 파트너 다른상품 리스트에 판매가 노출:
   BE PublicProductController partnerProducts 맵에 salePrice 추가. FE 카드에 won(salePrice) 표시(그린 볼드).
■ 2. 핵심스펙/효과·영업대상·상품특성·처리프로세스 '보이지 않음' 점검:
   BE(Request/dto/apply·public detail m.put)·admin ProductForm(state·payload·RichTextEditor 4종)·HOME RichBlock
   전 구간 연결 확인 완료. 등록된 내용이 있을 때만 노출(내용 없으면 섹션 자동 숨김) = 정상 동작.
   → 각 상품 편집화면에서 해당 에디터에 내용 입력 후 저장하면 상세화면에 노출됨.
■ 3. [이 상품으로 수익을 만드는 2가지 방법] 고정영역 신설(goods_view.docx image7 · 04/REVENUE):
   OPTION A·CP(오렌지, 버즈회원 '주변 사장님께 추천만 하세요!' 추천성공시 건당 최대 buzzRewardWon)
   + OPTION B·MP(블루, 관리매니저 '현장 방문 및 실사·계약 전담' 이행완료시 건당 managerRewardWon).
   수당 금액은 상품 등록수당(원 환산)으로 자동 반영. 하단 '* CP·MP 포인트는 친비즈 정책에 따라 적립' 주석.
■ 4. 하단 CTA 버튼 영역을 고정(fixed)에서 본문 내 영역으로 이동:
   기존 fixed inset-x-0 bottom-0 하단바 제거 → 본문 파트너다른상품 아래 카드형 섹션으로 배치.
   본문 pb-28 예약패딩 제거. 토스트 위치 bottom-8로 조정. 역할별 CTA 로직 동일 유지.
■ 검증: BE 컴파일 0·HOME tsc 0. 서버 반영: BE 재기동(salePrice) + home 재빌드.

[버그수정] 상품상세 확장4종/상세설명 — 이미지만 등록 시 미노출 (2026-08-28)
====================================================================
■ 원인1(섹션 숨김): RichBlock/상세설명의 '빈 내용' 판정이 태그 제거 후 텍스트만 검사 →
   이미지만 등록한 경우(텍스트 0) 빈 값으로 간주되어 섹션 자동 숨김.
   → hasContent(): <img|iframe|video|source|embed|svg|picture> 있으면 콘텐츠로 인정하도록 수정.
■ 원인2(이미지 깨짐): 에디터가 삽입한 <img src="/uploads/..">(상대경로)가 HOME(8001)에선 BE(9001)로 안 붙음.
   → rehostHtml(): 본문 HTML의 모든 src 를 mediaUrl()로 재구성(현재 API_BASE). YouTube 등 외부 절대URL은 그대로.
   description·확장4종 렌더에 rehostHtml 적용.
■ 스타일: home globals.css 에 .rte-content(img max-width:100% 등) 추가 — 임베드 이미지 반응형/오버플로 방지.
■ 검증: HOME tsc 0. 서버 반영: home 재빌드.

[추가] 본사 외 admin 상품 정보확인 화면에 확장필드 노출 (2026-08-28)
====================================================================
대상: 버즈/매니저·파트너 (본부·센터 마켓은 placeholder라 제외). 노출 항목:
  월관리상품/AS지원 배지·상품영상(임베드)·핵심스펙효과·영업대상·상품특성·처리프로세스.
■ 버즈/매니저 상품상세(components/buzz/ProductDetail.tsx + BE BuzzMarketController.detail):
   BE /api/buzz/products/{id} 응답에 videoUrl·monthlyCare·asSupport·spec/target/feature/process 추가.
   FE: 상단 카드 우측에 월관리상품/AS지원 배지, '상품 동영상' 카드(YouTube/Vimeo 임베드),
     '상품 설명 및 규정' 아래 확장4종 카드(내용 있을때만). 이미지 src 는 richHtml()로 mediaUrl 재구성+sanitize.
■ 파트너 상품정보(components/partner/ProductForm.tsx = edit는 보기전용 + BE PartnerProductController):
   BE dto()에 확장필드 반환, ProductRequest/apply()에 7필드 추가(신규등록 시 저장).
   FE: 기본정보에 상품특성(월관리/AS) 토글, 설명영역에 상품영상URL 입력 + 확장4종 RichTextEditor.
■ 검증: BE 컴파일 0·admin tsc 0. 서버 반영: BE 재기동 + admin 재빌드.

[추가] 버즈 상품상세 — 상품유형 4종 플래그 함께 표시 (2026-08-28)
====================================================================
ProductDetail.tsx 상단 카드 우측 배지에 설치상품·단순배송·월관리상품·AS지원 4종을 함께 노출
  (색상: 설치=violet, 단순배송=slate, 월관리=emerald, AS지원=sky). 기존 D-day·수당유형 배지 유지.
'상품 설명 및 규정' > 상품유형 안내 블록에도 월관리/AS 안내문 추가(설치·단순배송·취소보전비와 함께).
BE 무변경(buzz 응답에 이미 4종 포함). 검증: admin tsc 0. FE-only(HMR 반영).

[추가] 파트너 대시보드 신설 + 상단 [대시보드] 메뉴 (partner.docx p.2~3) (2026-08-28)
====================================================================
■ 상단메뉴: PartnerTopbar NAV 최상단에 [대시보드](/partner/dashboard) 추가.
■ 신규 페이지 app/partner/dashboard/page.tsx = 2개 영역:
  ① CommissionProductsSection(위탁 영업대행 상품 및 총수당 관리):
     요약 3카드(등록상품수/총수당설정완료/활성상품) + 상품별 총수당 목록 테이블
     (상품코드·상품명·카테고리·공급가/판매가·총수당(위탁비)·수당조건변경).
  ② LiveSalesSection(실시간 상품 영업 및 고객정보 현황):
     요약 4카드(전체유입/진행중/완결/취소반품) + 필터탭(전체·진행중·계약완료·설치완료·취소반품)
     + 고객정보 목록(고객사·접수상품·1차버즈·2차매니저·진행상태 배지·최종업데이트).
  현재 mock 데이터(디자인 일치). BE 무변경. 검증: admin tsc 0, /partner/dashboard 200.

[수정] 파트너 대시보드 위탁상품 목록 — [수당/조건변경]→[상품보기]+실데이터 (2026-08-28)
====================================================================
CommissionProductsSection: mock→실데이터 전환(/api/partner/products·/categories 매핑).
버튼 [수당/조건 변경]→[상품보기], 클릭 시 /partner/products/{id}(상품 상세, 보기전용) 이동.
요약 3카드도 실데이터 기준 계산. 검증: admin tsc 0, /partner/dashboard 200.
(LiveSalesSection은 요청 범위 외 → mock 유지)

[수정] 파트너 상품상세보기 — 역할별 수당 영역 숨김 (2026-08-28)
====================================================================
partner ProductForm 역할별 수당 섹션을 {mode !== "edit"}로 감싸 상품상세보기(edit=보기전용)에서 제거.
신규등록(new)에서는 유지(7주체 배분 100% 검증 필요). 검증: admin tsc 0.

[추가] HOME 상품상세 하단 CTA — 버즈/매니저 역할별 버튼 (2026-08-30)
====================================================================
■ BE: PublicProductController.detail 응답에 categoryId 추가(1차영업 신청 자동선택용).
■ FE home /products/[id]:
  - resolveAdminUrl() 로 크로스앱 링크 applyHref = {ADMIN}/buzz/pipeline/new?productId={id}&categoryId={catId}.
  - BUZZ: [가망고객 접수하기](→applyHref) + [URL 복사].
  - MANAGER: [가망고객 접수하기](→applyHref) + [📷 설치완료 사진찍기].
    사진찍기 = 모바일(userAgent)면 hidden <input type=file accept=image/* capture=environment> 클릭(카메라 실행),
    PC면 "모바일에서 접속해 주세요" 토스트.
■ 검증: BE 컴파일 0, home tsc 0, /products/15 200, detail categoryId 반환 확인.

[수정] HOME 상품상세 파트너사 다른상품 — 4개씩 슬라이드 캐러셀 (2026-08-30)
====================================================================
■ BE: PublicProductController.detail partnerProducts 개수 5→12 (최근등록순).
■ FE: PartnerProductsCarousel 컴포넌트 — 4개씩 표시(w-1/4), 좌우 화살표(‹ ›)로 페이지 이동,
  flex 트랙 translateX(-page*100%) + transition-transform duration-300 ease-out 슬라이드 효과,
  하단 페이지 도트 인디케이터. 상품 4개 이하면 화살표/도트 숨김.
■ 검증: home tsc 0, /products/15 200, partnerProducts 6개 반환(>5) 확인.

[수정] 파트너사 다른상품 캐러셀 — 모바일 2개씩(2열) 반응형 (2026-08-30)
====================================================================
PartnerProductsCarousel: matchMedia("(min-width:640px)")로 perPage 결정(모바일 2 / sm↑ 4),
카드 폭 w-1/2 sm:w-1/4. translateX(-page*100%)는 화면당 개수와 무관하게 1화면씩 이동(폭 일치).
브레이크포인트 변경 시 page 클램프. FE-only. 검증: home tsc 0.

[추가] docs/25_2 — 매니저 상품상세 버튼 개편 + 현장설치 사진 (2026-08-30)
====================================================================
■ 1. [가망고객 접수하기](버즈·매니저 공통): admin 모드가 M이어도 B(버즈)모드로 진입.
   - home applyHref 에 view=buzz 추가. BuzzProvider가 URL ?view=buzz|manager 를 localStorage보다 우선 적용.
■ 2. [설치완료 사진찍기](매니저): 카메라 직접실행 → 레이어팝업으로 변경.
   - home: /api/buzz/sales/managed?productId={id} (매니저 토큰) 조회 → 등록일자·고객명·1차영업자·활동센터·영업단계 리스트.
   - 고객 클릭 → {ADMIN}/buzz/managed?assign={saleId}&view=manager 이동.
   - ManagerSalesSection: URL ?assign={id} → 진행관리(AssignModal) 자동 오픈.
   - BE: managed 엔드포인트에 productId 필터 파라미터 추가.
■ 3. 매니저 2차영업관리 [진행관리]→[영업권확보·관리](AssignModal)에 현장설치 사진 업로드:
   - 사진 최대 10장(모바일 카메라 capture=environment / PC 파일선택), /api/uploads 업로드→URL, 썸네일+삭제.
   - BE Sale.install_photos(TEXT) 추가, AssignRequest/assign 저장, detail 응답 반환.
■ 검증: BE 컴파일 0, home·admin tsc 0, install_photos 컬럼 자동생성 확인, 관련 라우트 200,
   managed?productId 인증게이트 403(정상). ⚠ 매니저 계정 로그인 E2E는 미수행(로컬 매니저 비번 미상).
   DB: migration_new_fields.sql + [DB 마이그레이션] 섹션에 sale.install_photos ALTER 기재.

[버그수정] 매니저 현장설치 사진 업로드 403 (2026-08-30)
====================================================================
증상: 영업권확보·관리 레이어에서 사진 등록 시 "사진 업로드에 실패했습니다".
원인: SecurityConfig /api/uploads 가 hasAnyRole("MASTER_ADMIN","PARTNER")로 제한 → MANAGER 403.
해결: /api/uploads 허용 역할에 BUZZ·MANAGER 추가(현장설치 사진 업로드).
검증: 임시 MANAGER 계정으로 POST /api/uploads → 201 {url} 확인(테스트 계정·파일 정리). BE 재기동 반영.

[⚠ 중요/주의] BE 로컬 실행 시 DB 접속 대상 (2026-08-30)
====================================================================
application.yml datasource url 기본값이 ${DB_HOST:175.125.94.198} = **원격(운영) DB**.
Spring Boot는 루트 .env 를 자동 로드하지 않으므로, 그냥 `mvnw spring-boot:run` 하면 **운영 DB(175.125.94.198)** 에 붙는다.
→ 로컬 개발 시 반드시 env 지정: `DB_HOST=localhost DB_USER=chin4_test DB_PASSWORD=1234 DB_NAME=chin4 mvnw ... spring-boot:run`.
(이번 세션 중 잠깐 운영DB로 기동된 적 있으나 GET 조회만 수행, 쓰기 없음 → 운영 데이터 무변경 확인. 이후 로컬로 재기동.)
